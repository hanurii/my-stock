# find-vcp Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SEPA 1단계(트렌드 통과) 종목에 대해 미너비니 VCP 베이스를 탐지하고 피벗·진입상태를 산출하는 스크리너(`find-vcp`)를 만든다.

**Architecture:** 순수 평가 부품 `scripts/canslim_lib/vcp.py`(ZigZag 스윙→수축수열→VCP판정)를 TDD로 만들고, CLI 오케스트레이터 `scripts/screen_vcp.py`가 `sepa-trend-candidates.json`을 입력받아 OHLCV 캐시로 종목별 평가 후 `sepa-vcp-candidates.json`을 쓴다. 마지막에 사용 안내 스킬 `find-vcp/SKILL.md`.

**Tech Stack:** Python 3.11+, pytest 9.x, 기존 `canslim_lib.ohlcv_matrix.get_series` (OHLCV 캐시 접근).

**Spec:** `docs/superpowers/specs/2026-06-29-find-vcp-design.md`

## Global Constraints

- 입력은 `sepa-trend-candidates.json`의 `all_pass == true` 종목만. 전 종목 스캔 아님.
- 공유 파일(`trend-template-candidates.json` 등) 무접촉. 출력은 `sepa-vcp-candidates.json` 전용.
- 컷오프(시총·거래대금·가격) 추가 금지.
- 환각 금지: 모든 판정 근거(스윙점·수축 깊이·사유)를 출력 JSON에 포함.
- 자동 git commit/push 안 함(스킬이 데이터 배포 트리거하지 않음).
- 모든 임계값은 CLI 인자 + `vcp.py`의 `DEFAULT_PARAMS`로 노출. 기본값: lookback_days=120, zigzag_pct=8.0, min_base_days=10, contraction_tol=1.15, max_final_depth=10.0, breakout_vol_mult=1.4, near_pivot_pct=5.0, base_vol_cap=50.
- Windows 콘솔 안전: 스크립트 상단에 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` (기존 스크립트와 동일 패턴).

---

### Task 1: vcp.py — ZigZag 스윙 탐지

**Files:**
- Create: `scripts/canslim_lib/vcp.py`
- Test: `tests/test_vcp.py`

**Interfaces:**
- Produces: `zigzag(values: list[float], pct: float) -> list[tuple[int, float, str]]`
  — 시간순 교대 피벗 `(index, price, kind)`, kind ∈ {"high","low"}. 시작점과 마지막 진행중 극점을 포함.
- Produces: `DEFAULT_PARAMS: dict` (Global Constraints의 기본값).

- [ ] **Step 1: Write the failing test**

```python
# tests/test_vcp.py
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "scripts"))

from canslim_lib.vcp import zigzag


def test_zigzag_picks_alternating_pivots_from_peak():
    # 100에서 시작(고점) → 80(-20%)으로 하락 → 92(+15%)로 반등 → 85로 마감
    values = [100, 95, 88, 80, 84, 90, 92, 88, 85]
    piv = zigzag(values, pct=8.0)
    kinds = [k for _, _, k in piv]
    # 시작 고점, 저점, 고점 ... 교대
    assert kinds[0] == "high"
    assert piv[0][1] == 100
    # 80 저점이 잡혀야
    assert any(abs(p - 80) < 1e-9 and k == "low" for _, p, k in piv)
    # 교대성: 같은 종류 연속 없음
    assert all(kinds[i] != kinds[i+1] for i in range(len(kinds)-1))


def test_zigzag_ignores_subthreshold_noise():
    # 100 -> 97(-3%, 임계 미만) -> 101 : 잡음이라 중간 저점 안 잡힘
    values = [100, 97, 101, 99, 103]
    piv = zigzag(values, pct=8.0)
    lows = [p for _, p, k in piv if k == "low"]
    assert 97 not in lows
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_vcp.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'canslim_lib.vcp'`

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/canslim_lib/vcp.py
"""미너비니 VCP(변동성 수축 패턴) 평가 부품 (순수 함수).

개념(VCP·피벗·수축·거래량 마름)=마크 미너비니. 구체 수치·계산규칙=이 프로젝트의
공학적 번역(원전 아님). 정의·근거: docs/superpowers/specs/2026-06-29-find-vcp-design.md
"""
from __future__ import annotations

DEFAULT_PARAMS: dict = {
    "lookback_days": 120,
    "zigzag_pct": 8.0,
    "min_base_days": 10,
    "contraction_tol": 1.15,
    "max_final_depth": 10.0,
    "breakout_vol_mult": 1.4,
    "near_pivot_pct": 5.0,
    "base_vol_cap": 50,
}


def zigzag(values: list[float], pct: float) -> list[tuple[int, float, str]]:
    """퍼센트-역행 ZigZag. 교대 피벗 (index, price, kind) 리스트.

    시작점은 첫 확정 레그 방향에 따라 high/low 로 포함되고, 끝에는 진행 중인
    마지막 극점을 닫는 피벗으로 추가한다.
    """
    n = len(values)
    if n == 0:
        return []
    if n == 1:
        return [(0, values[0], "high")]
    thr = pct / 100.0
    pivots: list[tuple[int, float, str]] = []
    ext_idx, ext_val = 0, values[0]
    direction = 0  # 0 미정, 1 상승레그(ext=고점후보), -1 하락레그(ext=저점후보)
    for i in range(1, n):
        v = values[i]
        if direction == 0:
            if v >= ext_val * (1 + thr):
                pivots.append((0, values[0], "low"))
                direction, ext_idx, ext_val = 1, i, v
            elif v <= ext_val * (1 - thr):
                pivots.append((0, values[0], "high"))
                direction, ext_idx, ext_val = -1, i, v
            elif v > ext_val:
                ext_idx, ext_val = i, v
        elif direction == 1:
            if v > ext_val:
                ext_idx, ext_val = i, v
            elif v <= ext_val * (1 - thr):
                pivots.append((ext_idx, ext_val, "high"))
                direction, ext_idx, ext_val = -1, i, v
        else:  # direction == -1
            if v < ext_val:
                ext_idx, ext_val = i, v
            elif v >= ext_val * (1 + thr):
                pivots.append((ext_idx, ext_val, "low"))
                direction, ext_idx, ext_val = 1, i, v
    closing = "high" if direction == 1 else "low"
    pivots.append((ext_idx, ext_val, closing))
    return pivots
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_vcp.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/canslim_lib/vcp.py tests/test_vcp.py
git commit -m "feat(vcp): ZigZag 스윙 탐지 부품 + 테스트"
```

---

### Task 2: vcp.py — 수축 수열 추출

**Files:**
- Modify: `scripts/canslim_lib/vcp.py`
- Test: `tests/test_vcp.py`

**Interfaces:**
- Consumes: `zigzag(...)` from Task 1.
- Produces: `find_contractions(pivots: list[tuple[int, float, str]]) -> list[float]`
  — 인접 (고점→다음 저점)마다 깊이% `(high-low)/high*100`, 시간순 리스트.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_vcp.py 에 추가
from canslim_lib.vcp import find_contractions


def test_find_contractions_high_to_low_depths():
    pivots = [
        (0, 100.0, "high"), (5, 75.0, "low"),
        (10, 90.0, "high"), (15, 78.0, "low"),
        (20, 86.0, "high"), (25, 80.0, "low"),
    ]
    depths = find_contractions(pivots)
    # (100-75)/100=25, (90-78)/90=13.33, (86-80)/86=6.98
    assert len(depths) == 3
    assert abs(depths[0] - 25.0) < 1e-6
    assert abs(depths[1] - 13.3333) < 1e-3
    assert abs(depths[2] - 6.9767) < 1e-3


def test_find_contractions_empty_when_no_pairs():
    assert find_contractions([(0, 100.0, "high")]) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_vcp.py::test_find_contractions_high_to_low_depths -v`
Expected: FAIL — `ImportError: cannot import name 'find_contractions'`

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/canslim_lib/vcp.py 에 추가
def find_contractions(pivots: list[tuple[int, float, str]]) -> list[float]:
    """인접한 (고점 → 바로 다음 저점) 쌍의 되돌림 깊이%를 시간순으로."""
    depths: list[float] = []
    for a, b in zip(pivots, pivots[1:]):
        if a[2] == "high" and b[2] == "low":
            high, low = a[1], b[1]
            if high > 0:
                depths.append((high - low) / high * 100.0)
    return depths
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_vcp.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add scripts/canslim_lib/vcp.py tests/test_vcp.py
git commit -m "feat(vcp): 수축 수열 추출 + 테스트"
```

---

### Task 3: vcp.py — VCP 종합 판정 evaluate_vcp

**Files:**
- Modify: `scripts/canslim_lib/vcp.py`
- Test: `tests/test_vcp.py`

**Interfaces:**
- Consumes: `zigzag`, `find_contractions`, `DEFAULT_PARAMS`.
- Produces: `evaluate_vcp(series: dict, params: dict | None = None) -> dict`
  - `series`: `{"dates": list[str], "closes": list[float], "highs": list[float], "lows": list[float], "volumes": list[int]}`
  - 반환 dict 키: `vcp_detected`(bool), `num_contractions`(int), `contractions`(list[float]),
    `base_length_days`(int), `base_depth_pct`(float|None), `pivot_price`(float|None),
    `pct_to_pivot`(float|None), `volume_dryup_ratio`(float|None), `tightness_pct`(float|None),
    `status`(str: breakout|actionable|forming|failed), `swings`(list[dict]), `reason`(str|None).

판정 규칙(spec §4):
- 베이스 시작 = 최근 `lookback_days` 종가 구간에서 **최고 종가** 인덱스. 베이스 = 그 지점~끝.
- 베이스 길이 < `min_base_days` → `vcp_detected=False, reason="base_too_short", status="forming"`.
- 스윙 = `zigzag(base_closes, zigzag_pct)`. 수축 = `find_contractions(swings)`.
- `base_vol_avg` = 베이스 거래량 평균(베이스가 `base_vol_cap`보다 길면 최근 `base_vol_cap`일).
- VCP 4조건: ① 2 ≤ T ≤ 6, ② 단조수렴 `depth[i] <= depth[i-1]*contraction_tol`,
  ③ 거래량 마름: 베이스 후반 1/3 평균 < 전반 1/3 평균, ④ 최종 수축 ≤ `max_final_depth`.
- 피벗 = 마지막 "high" 스윙 가격. `pct_to_pivot=(pivot-last_close)/pivot*100`.
- `volume_dryup_ratio = mean(최근5 volumes)/base_vol_avg`.
- `tightness_pct = mean((high-low)/close*100, 최근10일)`.
- status: `breakout`(last_close>pivot AND last_vol>=base_vol_avg*breakout_vol_mult) →
  `failed`(수축 마지막이 직전보다 깊음, 즉 단조수렴 위반 AND 수축≥2 / 또는 last_close<베이스 최저저점) →
  `actionable`(0<=pct_to_pivot<=near_pivot_pct AND volume_dryup_ratio<=1.0) → 그 외 `forming`.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_vcp.py 에 추가
from canslim_lib.vcp import evaluate_vcp


def _series_from_closes(closes):
    # highs/lows = 종가 ±0.5%, volumes 균일(거래량 마름 테스트는 개별 지정)
    highs = [c * 1.005 for c in closes]
    lows = [c * 0.995 for c in closes]
    vols = [1000] * len(closes)
    dates = [f"2026-01-{i+1:02d}" for i in range(len(closes))]
    return {"dates": dates, "closes": closes, "highs": highs, "lows": lows, "volumes": vols}


def _vcp_closes():
    # 고점100 → -25% → 회복 → -13% → 회복 → -7% (수축 수렴) → 피벗 근처
    seg = []
    seg += [100, 92, 84, 78, 75]          # -25%
    seg += [80, 86, 90]                    # 회복
    seg += [88, 84, 80, 78.3]              # -13% (90->78.3)
    seg += [82, 86, 88]                    # 회복
    seg += [87, 85, 83, 81.8]              # -7% (88->81.8)
    seg += [84, 86]                        # 피벗(88) 향해 접근
    return seg


def test_evaluate_vcp_detects_contracting_base():
    s = _series_from_closes(_vcp_closes())
    # 거래량 마름: 후반 1/3 거래량을 낮춤
    third = len(s["volumes"]) // 3
    s["volumes"] = [1500]*third + [1000]*third + [600]*(len(s["volumes"])-2*third)
    r = evaluate_vcp(s)
    assert r["vcp_detected"] is True
    assert 2 <= r["num_contractions"] <= 6
    # 수축이 대체로 얕아지는 수열
    assert r["contractions"][0] > r["contractions"][-1]
    assert r["pivot_price"] is not None


def test_evaluate_vcp_rejects_short_base():
    s = _series_from_closes([100, 98, 99, 97, 98])  # 5일 < min_base_days
    r = evaluate_vcp(s)
    assert r["vcp_detected"] is False
    assert r["reason"] == "base_too_short"


def test_evaluate_vcp_breakout_status():
    closes = _vcp_closes() + [89.0]   # 피벗(88) 위로 돌파
    s = _series_from_closes(closes)
    s["volumes"][-1] = 5000           # 돌파 거래량 급증
    r = evaluate_vcp(s)
    assert r["status"] == "breakout"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_vcp.py -k evaluate_vcp -v`
Expected: FAIL — `ImportError: cannot import name 'evaluate_vcp'`

- [ ] **Step 3: Write minimal implementation**

```python
# scripts/canslim_lib/vcp.py 에 추가
def _mean(xs: list[float]) -> float | None:
    xs = [x for x in xs if x is not None]
    return sum(xs) / len(xs) if xs else None


def evaluate_vcp(series: dict, params: dict | None = None) -> dict:
    p = {**DEFAULT_PARAMS, **(params or {})}
    closes = (series.get("closes") or [])[-p["lookback_days"]:]
    highs = (series.get("highs") or [])[-p["lookback_days"]:]
    lows = (series.get("lows") or [])[-p["lookback_days"]:]
    vols = (series.get("volumes") or [])[-p["lookback_days"]:]
    dates = (series.get("dates") or [])[-p["lookback_days"]:]

    base = {
        "vcp_detected": False, "num_contractions": 0, "contractions": [],
        "base_length_days": 0, "base_depth_pct": None, "pivot_price": None,
        "pct_to_pivot": None, "volume_dryup_ratio": None, "tightness_pct": None,
        "status": "forming", "swings": [], "reason": None,
    }
    if len(closes) < p["min_base_days"]:
        base["reason"] = "no_data" if not closes else "base_too_short"
        return base

    # 베이스 시작 = lookback 내 최고 종가 지점
    start = max(range(len(closes)), key=lambda i: closes[i])
    bc, bh, bl, bv, bd = closes[start:], highs[start:], lows[start:], vols[start:], dates[start:]
    base["base_length_days"] = len(bc)
    if len(bc) < p["min_base_days"]:
        base["reason"] = "base_too_short"
        return base

    swings = zigzag(bc, p["zigzag_pct"])
    base["swings"] = [{"date": bd[i] if i < len(bd) else None, "price": round(pr, 2), "kind": k}
                      for i, pr, k in swings]
    contractions = find_contractions(swings)
    base["contractions"] = [round(d, 2) for d in contractions]
    base["num_contractions"] = len(contractions)

    cap = p["base_vol_cap"]
    base_vol_avg = _mean(bv[-cap:] if len(bv) > cap else bv) or 0.0
    last_close = bc[-1]

    # 피벗 = 마지막 high 스윙
    highs_sw = [pr for _, pr, k in swings if k == "high"]
    pivot = highs_sw[-1] if highs_sw else None
    base["pivot_price"] = round(pivot, 2) if pivot else None
    if pivot:
        base["pct_to_pivot"] = round((pivot - last_close) / pivot * 100.0, 2)

    base["volume_dryup_ratio"] = round((_mean(bv[-5:]) or 0.0) / base_vol_avg, 3) if base_vol_avg else None
    tight = _mean([(bh[i] - bl[i]) / bc[i] * 100.0 for i in range(len(bc))[-10:] if bc[i]])
    base["tightness_pct"] = round(tight, 2) if tight is not None else None

    # VCP 4조건
    T = len(contractions)
    cond_count = 2 <= T <= 6
    cond_mono = all(contractions[i] <= contractions[i-1] * p["contraction_tol"]
                    for i in range(1, T)) if T >= 2 else False
    third = max(1, len(bv) // 3)
    early_vol = _mean(bv[:third]) or 0.0
    late_vol = _mean(bv[-third:]) or 0.0
    cond_volcontract = late_vol < early_vol
    cond_final_tight = (contractions[-1] <= p["max_final_depth"]) if T >= 1 else False
    base["vcp_detected"] = bool(cond_count and cond_mono and cond_volcontract and cond_final_tight)
    if base["vcp_detected"]:
        base["base_depth_pct"] = round(max(contractions), 2)

    # 상태 판정
    last_vol = bv[-1] if bv else 0.0
    base_low = min(bl) if bl else last_close
    mono_violated = T >= 2 and contractions[-1] > contractions[-2] * p["contraction_tol"]
    if pivot and last_close > pivot and base_vol_avg and last_vol >= base_vol_avg * p["breakout_vol_mult"]:
        base["status"] = "breakout"
    elif mono_violated or last_close < base_low:
        base["status"] = "failed"
    elif (base["pct_to_pivot"] is not None and 0 <= base["pct_to_pivot"] <= p["near_pivot_pct"]
          and (base["volume_dryup_ratio"] or 9.9) <= 1.0):
        base["status"] = "actionable"
    else:
        base["status"] = "forming"
    return base
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_vcp.py -v`
Expected: PASS (7 passed). 실패 시 합성 시계열(`_vcp_closes`)의 깊이/거래량을 조정해 조건을 만족시키고, 알고리즘 버그면 `evaluate_vcp`를 고친다.

- [ ] **Step 5: Commit**

```bash
git add scripts/canslim_lib/vcp.py tests/test_vcp.py
git commit -m "feat(vcp): VCP 종합 판정 evaluate_vcp + 테스트"
```

---

### Task 4: screen_vcp.py — CLI 오케스트레이터

**Files:**
- Create: `scripts/screen_vcp.py`

**Interfaces:**
- Consumes: `canslim_lib.vcp.evaluate_vcp`, `canslim_lib.vcp.DEFAULT_PARAMS`,
  `canslim_lib.ohlcv_matrix.get_series(code) -> dict | None`.
- Produces: `public/data/sepa-vcp-candidates.json` (spec §5 스키마).

- [ ] **Step 1: Write the script**

```python
# scripts/screen_vcp.py
"""find-vcp — SEPA 2단계: 트렌드 통과 종목의 VCP 베이스·피벗 탐지.

입력: public/data/sepa-trend-candidates.json (all_pass 종목)
출력: public/data/sepa-vcp-candidates.json
정의: docs/superpowers/specs/2026-06-29-find-vcp-design.md
"""
from __future__ import annotations

import argparse
import json
import sys
from datetime import datetime, timezone, timedelta
from pathlib import Path

try:
    sys.stdout.reconfigure(encoding="utf-8", errors="replace")
    sys.stderr.reconfigure(encoding="utf-8", errors="replace")
except Exception:
    pass

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "scripts"))

from canslim_lib import ohlcv_matrix  # noqa: E402
from canslim_lib.vcp import evaluate_vcp, DEFAULT_PARAMS  # noqa: E402

KST = timezone(timedelta(hours=9))
IN_PATH = ROOT / "public" / "data" / "sepa-trend-candidates.json"
OUT_PATH = ROOT / "public" / "data" / "sepa-vcp-candidates.json"
STATUS_ORDER = {"breakout": 0, "actionable": 1, "forming": 2, "failed": 3}


def run(args) -> None:
    in_path = Path(args.inp) if args.inp else IN_PATH
    if not in_path.is_absolute():
        in_path = ROOT / in_path
    data = json.loads(in_path.read_text(encoding="utf-8"))
    passers = [c for c in data.get("candidates", []) if c.get("all_pass")]
    if args.ticker:
        passers = [c for c in passers if c.get("code") == args.ticker]

    params = {
        "lookback_days": args.lookback_days, "zigzag_pct": args.zigzag_pct,
        "max_final_depth": args.max_final_depth, "breakout_vol_mult": args.breakout_vol_mult,
        "near_pivot_pct": DEFAULT_PARAMS["near_pivot_pct"],
    }
    out_cands = []
    for c in passers:
        code = c["code"]
        s = ohlcv_matrix.get_series(code)
        if not s or not s.get("closes"):
            r = {"vcp_detected": False, "status": "forming", "reason": "no_series",
                 "num_contractions": 0, "contractions": [], "pivot_price": None,
                 "pct_to_pivot": None, "volume_dryup_ratio": None, "tightness_pct": None,
                 "base_length_days": 0, "base_depth_pct": None, "swings": []}
        else:
            r = evaluate_vcp(s, params)
        out_cands.append({
            "code": code, "name": c.get("name"), "market": c.get("market"),
            "current_price": c.get("current_price"), "rs": c.get("rs"),
            **r,
        })

    out_cands.sort(key=lambda x: (STATUS_ORDER.get(x["status"], 9),
                                  x["pct_to_pivot"] if x["pct_to_pivot"] is not None else 1e9))
    dist = {k: sum(1 for x in out_cands if x["status"] == k)
            for k in ("breakout", "actionable", "forming", "failed")}
    output = {
        "generated_at": datetime.now(KST).strftime("%Y-%m-%d %H:%M"),
        "asof": data.get("asof"),
        "source": in_path.name,
        "params": params,
        "vcp_count": sum(1 for x in out_cands if x["vcp_detected"]),
        "status_distribution": dist,
        "candidates": out_cands,
    }

    if not args.ticker:
        OUT_PATH.parent.mkdir(parents=True, exist_ok=True)
        OUT_PATH.write_text(json.dumps(output, ensure_ascii=False, indent=2), encoding="utf-8")
        print(f"\n💾 저장: {OUT_PATH.relative_to(ROOT)}")

    print(f"\n[VCP 요약] 입력 {len(passers)}종목 | VCP {output['vcp_count']} | "
          f"breakout {dist['breakout']} · actionable {dist['actionable']} · "
          f"forming {dist['forming']} · failed {dist['failed']}")
    for x in out_cands:
        if x["status"] in ("breakout", "actionable"):
            print(f"  [{x['status']:10s}] {x['code']} {str(x['name'])[:12]:12s} "
                  f"T={x['num_contractions']} 피벗 {x['pivot_price']} "
                  f"→ {x['pct_to_pivot']}% dryup {x['volume_dryup_ratio']}")


def main():
    ap = argparse.ArgumentParser(description="find-vcp — VCP 베이스·피벗 탐지")
    ap.add_argument("--in", dest="inp", default=None, help=f"입력(default {IN_PATH.name})")
    ap.add_argument("--out", dest="out", default=None, help=f"출력(default {OUT_PATH.name})")
    ap.add_argument("--ticker", default=None, help="단일 종목 디버그(저장 안 함)")
    ap.add_argument("--lookback-days", type=int, default=DEFAULT_PARAMS["lookback_days"])
    ap.add_argument("--zigzag-pct", type=float, default=DEFAULT_PARAMS["zigzag_pct"])
    ap.add_argument("--max-final-depth", type=float, default=DEFAULT_PARAMS["max_final_depth"])
    ap.add_argument("--breakout-vol-mult", type=float, default=DEFAULT_PARAMS["breakout_vol_mult"])
    args = ap.parse_args()
    global OUT_PATH
    if args.out:
        OUT_PATH = Path(args.out) if Path(args.out).is_absolute() else ROOT / args.out
    run(args)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Smoke-run on a single ticker (no save)**

Run: `python scripts/screen_vcp.py --ticker 240810`
Expected: 에러 없이 `[VCP 요약]` 한 줄 출력(입력 1종목). 240810이 입력에 없으면 다른 통과 종목 코드로 대체.

- [ ] **Step 3: Full run on real input**

Run: `python scripts/screen_vcp.py`
Expected: `💾 저장: public\data\sepa-vcp-candidates.json` + 요약. 상태 분포가 합리적(대부분 forming, 소수 actionable/breakout/failed).

- [ ] **Step 4: Sanity-check output JSON**

Run:
```bash
python -X utf8 -c "import json;d=json.load(open('public/data/sepa-vcp-candidates.json',encoding='utf-8'));print(d['status_distribution'],'vcp',d['vcp_count'],'n',len(d['candidates']))"
```
Expected: 입력 통과 종목 수와 `len(candidates)` 일치, 분포 합 = n.

- [ ] **Step 5: Commit**

```bash
git add scripts/screen_vcp.py public/data/sepa-vcp-candidates.json
git commit -m "feat(vcp): screen_vcp CLI + 첫 산출"
```

---

### Task 5: find-vcp 스킬 문서

**Files:**
- Create: `.claude/skills/find-vcp/SKILL.md`

- [ ] **Step 1: Write the skill**

```markdown
---
name: find-vcp
description: >
  SEPA 2단계. 1단계(find-trend-template) 통과 종목의 일봉에서 미너비니 VCP(변동성
  수축 패턴) 베이스를 탐지하고 피벗(돌파 매수점)·진입상태(breakout/actionable/
  forming/failed)를 산출해 sepa-vcp-candidates.json 에 저장한다. OHLCV 캐시만 사용,
  수급·공유 파일 무접촉. 사용자가 "/find-vcp", "VCP 찾아줘", "베이스·피벗 분석",
  "SEPA 2단계" 등을 요청할 때 사용.
---

# find-vcp — SEPA 2단계: VCP 베이스·피벗 탐지

`find-trend-template`(SEPA 1단계) 통과 종목에 대해 미너비니 **VCP(변동성 수축
패턴)** 를 탐지한다. 정의·근거: `docs/superpowers/specs/2026-06-29-find-vcp-design.md`.

## 사전 조건
- **최신 데이터로 돌리려면 먼저 `update-data` → `find-trend-template`** 실행.
- 입력 `public/data/sepa-trend-candidates.json` 존재(= find-trend-template 산출).

## 실행 (1줄)
```
python scripts/screen_vcp.py
```
- 산출: `public/data/sepa-vcp-candidates.json`
- 콘솔: 상태 분포 + breakout/actionable 종목 표.

### 옵션
- `--ticker CODE` : 단일 종목 디버그(저장 안 함).
- `--zigzag-pct 8` / `--max-final-depth 10` / `--breakout-vol-mult 1.4` /
  `--lookback-days 120` : VCP 임계값 튜닝(기본값은 미너비니 개념 기반 추정).
- `--out PATH` : 출력 경로 변경.

## 결과 확인
- `status_distribution` : breakout(돌파 중) · actionable(피벗 근접+거래량 마름) ·
  forming(형성 중) · failed(수렴 실패).
- `actionable`/`breakout` 종목이 다음 단계(리스크·진입) 후보.

## 안 하는 것
- 전 종목 스캔(트렌드 통과 종목만) · 공유 파일 갱신 · 수급 신호 · 자동 commit.
- 프로젝트 5신호(눌림목 재가속) — 별도. 이번은 교과서 VCP만.
```

- [ ] **Step 2: Verify skill is discoverable**

Run: `python -c "import pathlib,re; t=pathlib.Path('.claude/skills/find-vcp/SKILL.md').read_text(encoding='utf-8'); assert t.startswith('---') and 'name: find-vcp' in t; print('skill ok')"`
Expected: `skill ok`

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/find-vcp/SKILL.md
git commit -m "docs(vcp): find-vcp 스킬 문서"
```

---

## Self-Review 결과 (작성자 점검)

- **Spec 커버리지**: §3 입력(Task4) · §3.5 출처(스킬·vcp.py 주석) · §4 알고리즘(Task1~3) ·
  §5 출력 스키마(Task4) · §6 코드구조(vcp.py+screen_vcp.py, Task1~4) · §7 불변원칙(Global
  Constraints) · §8 검증(Task3 단위테스트·Task4 풀런) 모두 태스크 존재.
- **타입 일관성**: `evaluate_vcp(series, params)` 반환 키가 Task4 출력 dict·spec §5와 일치.
  `zigzag`/`find_contractions` 시그니처 Task1·2와 Task3 사용 일치.
- **미해결**: 합성 시계열 테스트는 임계 만족하도록 깊이/거래량을 구성했으나, 첫 실행에서
  경계값이 빗나가면 Task3 Step4 지침대로 시계열 수치를 조정(알고리즘 버그 아닌 한).
```
