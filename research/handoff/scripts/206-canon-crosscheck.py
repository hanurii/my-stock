# -*- coding: utf-8 -*-
r"""206 — **canon 538줄 대조 · 범위는 「Ⓓ4 «찾기»」** · 두뇌·검증 의뢰 2026-09-07

  🎯 **찾을 것은 «하나»** —
     「**«주석»이 «출처»를 «주장»하는데 — canon 이 «다르게» 말하거나 «없다»고 말하는**」 것.
     ⇒ ★ **«고칠 수» 있는 «유일»한 칸**이라 값진다(«행동»이 «바뀐다»).

  ⛔ **Ⓓ2/Ⓓ3 는 «더» «세지» 않는다** — 검증 판정: 「«더» 세도 «같은» 말」 · 수확 «체감».
  ⛔ **canon 의 «등급»을 «그대로» 쓴다** — «우리»가 «다시» 매기지 «않는다**.

실행: PYTHONIOENCODING=utf-8 PYTHONUTF8=1 python research/handoff/scripts/206-canon-crosscheck.py
      (--pre 를 주면 «사전등록»만 찍는다)
"""
from __future__ import annotations

import re as _re
import sys
from pathlib import Path

sys.stdout.reconfigure(encoding="utf-8")
P = print
BQ = chr(96)
F3 = BQ * 3
HANDOFF = Path(__file__).resolve().parents[1]
CANON = HANDOFF / "canon" / "minervini-principles.md"

# ══════════════════════════════════════════════════════════════════════════════
# 사전등록 — **값 보기 «전»**(관문 ②·③)
# ══════════════════════════════════════════════════════════════════════════════
SYM_MORE = ("**Ⓓ4 가 «더» 나오면** → §G1 이 «늘고» — **«고칠» 목록이 «생긴다»**"
            "(«행동»이 «바뀐다»)")
SYM_NONE = ("**«하나»도 «안» 나오면** → **「Ⓓ4 «열하나»가 «전부»」**이고 "
            "— **§G 세기는 «닫힌다»**")

DISCLOSE = [
    "🚨 **«훑은» 것을 «먼저» 적는다**(관문 ③ · `205` 에서와 «같은» 규약)",
    "",
    "⚠️ `205` 를 지으며 **canon 의 «일부» 줄을 «이미» 봤다** — «전수»가 «아니라» «grep» 으로:",
    "   `:15-22`(등급 범례) · `:53`(52주 저가 30%) · `:58`(6번이 25/30 갈림) · `:70`(EPS 20/25/30)",
    "   `:76`(유통주식 문턱 «없음») · `:95`(VCP 140~150% = [3차]) · `:143` · `:153-155` · `:162`",
    "   `:174`(100%/8주/3~6주/20~25%) · `:187` · `:189`(기관 수급) · `:216`(거래량 «몇 배» «못» 찾음)",
    "   `:235-236`(손절 5값 · 「7~8%는 오닐」) · `:240`(손절 집행 [1차-전사]) · `:265` · `:270` · `:278-281`",
    "",
    "🔴 **그러니 「«새로» 찾았다」의 «일부»는 «이미» 본 것이다** — 표에 **「`205` 에서 «봄»」**을 «적는다**",
    "✅ **대칭 «자체»는 «오염되지» 않았다** — 위 둘을 «적은» 것은 **두뇌·검증**이고 «내» 훑기 «전»이다",
]

# ══════════════════════════════════════════════════════════════════════════════
# 맞대기 표 — (우리 자리, 값, 주석이 «주장»한 것, canon 이 «말하는» 것, 판정, 덮는 «값» 수)
# ══════════════════════════════════════════════════════════════════════════════
N_CANON_USED = 12          # canon 46 «등급» 자리 중 «걸어 본» 자리 수(:53·55·58·70·76·95·143·162·166·174·216·304)

ROWS = [
    (BQ + "trend_template.py:233" + BQ + " → " + BQ + "GATE_MARGIN_REF" + BQ, "8값",
     "「'충분' 기준은 **미너비니 «원전»의 «권장선»을 «그대로» 옮긴 값**」",
     "canon:55 「**가까울수록** 좋다」 · 8조건⑧ 「**강할수록** 좋다」 — **«방향»만 · «수» «없음»**",
     "🔴 **Ⓓ4**", 8),
    (BQ + "sell_rules.py:17" + BQ + " " + BQ + "UP_STREAK_IDEAL" + BQ, "7",
     "「연속 상승 이상적 기준**(미너비니)**」",
     "「연속 상승」을 **«안» 다룬다**(grep 「연속」 → :68 Code33 · :335 연속 «손실» 뿐)",
     "🟠 **Ⓓ5**", 1),
    (BQ + "strategy_params.py:13" + BQ, "—",
     "「**원전**: 「거래회전율이나 세금 «때문에» 매도를 결정하는 일은 없다」」 — **«따옴표» «인용»**",
     "「세금」·「회전」을 **«안» 다룬다**(grep **0건**)", "🟠 **Ⓓ5**", 1),
    (BQ + "power_play.py" + BQ + " " + BQ + "DEFAULT_PARAMS" + BQ, "8값",
     "🔎 `:3-4` 「구체 «수치» = 공학적 번역(**«원전» «아님»**)」 — **«주장»을 «안» 함**",
     "canon:166 「**100%** 이상을 **8주 미만**」 · 「**3~6주** · **20~25%**」 ⇒ 우리 90·70일·8~30일·20 = **«다름»**",
     "⚪ **선언으로 «면함»**", 8),
    (BQ + "vcp.py:271" + BQ + " 피벗 정의", "—",
     "「피벗 = 돌파 직전 «최종 타이트 코일»의 고점. **미너비니 «표준»**」",
     "canon §6① 「피벗 = **마지막 수축의 고점 = 저항선**」 — **«같다»**", "✅ **일치**", 1),
    (BQ + "vcp.py:15" + BQ + " " + BQ + "coil_breakout_vol_mult" + BQ, "1.5",
     "주석 「**MIK 예시 보정**」 — «원전»을 «주장»하지 «않음»",
     "canon:216 「**우리 정본의 1.5배는 «우리»가 «정한» 값입니다**」 — **canon 이 «이미» 정직히 적음**",
     "✅ **일치**", 1),
]


def main():
    pre_only = "--pre" in sys.argv
    P("# 206 — **canon 538줄 대조 · 범위는 「Ⓓ4 «찾기»」**")
    P("")
    P("> 조사 세션 · " + BQ + "research/handoff/scripts/206-canon-crosscheck.py" + BQ
      + " · **문서는 이 출력 그 자체**(유형 48)")
    P("")
    P("---")
    P("")
    P("# 0. **사전등록** — 값 보기 «전»에 «둘 다» 적는다")
    P("")
    P(F3)
    P("✅ " + SYM_MORE)
    P("🔴 " + SYM_NONE)
    P("")
    P("## 📐 **찾을 것 «하나**")
    P("   **Ⓓ4** = 「«주석»이 «출처»를 «주장»했는데 — canon 이 **«다르게»** 말하거나 **«없다»**고 말한다」")
    P("")
    P("⛔ **Ⓓ2/Ⓓ3 는 «세지» 않는다**(검증: 「«더» 세도 «같은» 말」 · 수확 «체감»)")
    P("⛔ **canon 의 «등급»([1차-본인]/[1차-전사]/[3차-해석])을 «그대로» 쓴다**")
    P("   — **«우리»가 «다시» 매기지 «않는다**")
    P("⛔ **「몇 중 몇」을 «형식»으로** — canon «등급» 자리 «몇» 중 «몇»을 걸었나 · 우리 값 «몇»과 맞댔나")
    P(F3)
    P("")
    for ln in DISCLOSE:
        P(ln)
    P("")
    P("---")
    P("")
    if pre_only:
        P("⏸️ **`--pre` — 사전등록만 찍었다. «아직» «안» 훑었다.**")
        return 0

    if not CANON.exists():
        P("🚨 **멈춘다** — " + BQ + str(CANON) + BQ + " 가 «없다»")
        return 3
    lines = CANON.read_text(encoding="utf-8").split("\n")
    tag = _re.compile(r"\[(1차-본인|1차-전사|3차-해석|검색요약|읽음)\]")
    num = _re.compile(r"[0-9]")
    tagged = [(i, ln) for i, ln in enumerate(lines, 1) if tag.search(ln)]
    tagged_num = [(i, ln) for i, ln in tagged if num.search(ln)]
    P("# 1. **자료 — 기계로 «센다»**")
    P("")
    P(F3)
    P("   **canon 줄 수 = %s**   ⟵  " % format(len(lines), ",") + BQ + "len(lines)" + BQ)
    P("   **«등급» 표시가 «붙은» 줄 = %d**   ⟵  " % len(tagged)
      + BQ + "re '[(1차-본인|1차-전사|3차-해석|검색요약|읽음)]'" + BQ)
    P("   **그중 «수»가 «있는» 줄 = %d**   ⟵  " % len(tagged_num) + BQ + "+ re '[0-9]'" + BQ)
    P(F3)
    P("")
    P("## 🔎 **«등급» 자리 전수**(줄 번호 · 앞 110자)")
    P("")
    P(F3)
    for i, ln in tagged:
        t = tag.search(ln).group(1)
        body = ln.strip()[:110]
        P("%3d [%s] %s" % (i, t, body))
    P(F3)
    P("")
    P("---")
    P("")
    P("# 2. **맞대기 — 「«주석»이 «주장»한 것」 vs 「canon 이 «말하는» 것」**")
    P("")
    P("## 📐 **판정 칸 «넷** — 🚨 **Ⓓ5 를 «새로» 갈랐다**")
    P("")
    P(F3)
    P("🔴 **Ⓓ4** — canon 이 **«다르게»** 말하거나 **«없다»고 «말한다»**   ⟵ 두뇌·검증이 준 칸")
    P("🟠 **Ⓓ5** — canon 이 그 «주제»를 **«안» 다룬다**(«내» grep 이 «못» 찾았다)")
    P("   🚨 **§F 의 그 규율 때문에 «갈랐다»**: 「«없다»」는 «내» «명령»이 «만든다**")
    P("   ⇒ ★ **「canon 에 «없다»」 ≠ 「canon 이 «없다»고 «말했다»」** — 뒤엣것만 Ⓓ4 다")
    P("⚪ **선언으로 «면함»** — 주석이 **「원전 «아님»」을 «스스로» 적었다**")
    P("✅ **일치**")
    P(F3)
    P("")
    P("| 우리 자리 | 값 | 주석이 «주장»한 것 | canon 이 «말하는» 것 | 판정 |")
    P("|---|---:|---|---|:--|")
    for a, b, cl, cn, v, _n in ROWS:
        P("| %s | %s | %s | %s | %s |" % (a, b, cl, cn, v))
    P("")
    n4 = sum(1 for r in ROWS if "Ⓓ4" in r[4])
    n5 = sum(1 for r in ROWS if "Ⓓ5" in r[4])
    nx = sum(1 for r in ROWS if "면함" in r[4])
    nok = sum(1 for r in ROWS if "일치" in r[4])
    nval = sum(r[5] for r in ROWS)
    P(F3)
    P("## ⇒ **「몇 중 몇」**(관문 ①)")
    P("")
    P("   canon «등급» 자리 **%d** 중 — «걸어 본» 자리 **%d**" % (len(tagged), N_CANON_USED))
    P("   우리 «주석» 주장 **%d** 줄 · 그 주석이 «덮는» «값» **%d** 개" % (len(ROWS), nval))
    P("")
    P("   🔴 **Ⓓ4**            **%d** 줄 · **%d** 값" %
      (n4, sum(r[5] for r in ROWS if "Ⓓ4" in r[4])))
    P("   🟠 **Ⓓ5**            **%d** 줄 · **%d** 값" %
      (n5, sum(r[5] for r in ROWS if "Ⓓ5" in r[4])))
    P("   ⚪ **선언으로 면함**  **%d** 줄 · **%d** 값" %
      (nx, sum(r[5] for r in ROWS if "면함" in r[4])))
    P("   ✅ **일치**          **%d** 줄 · **%d** 값" %
      (nok, sum(r[5] for r in ROWS if "일치" in r[4])))
    P("   ─────────────────────────────────────")
    P("   **합 %d / %d 줄**" % (n4 + n5 + nx + nok, len(ROWS)))
    P(F3)
    P("")
    P("---")
    P("")
    P("# ★★★ 3. **가장 큰 것 — " + BQ + "trend_template.py:233" + BQ + "**")
    P("")
    P(F3)
    P("주석: 「'충분' 기준은 **미너비니 «원전»의 «권장선»을 «그대로» 옮긴 값**이다」")
    P("")
    P("🔎 canon:55  「7. 현재가가 52주 «고가»의 25% 이내 **(고가에 «가까울수록» 좋다)**」")
    P("🔎 canon 8조건 8번 「지수 대비 상대 강도는 **«강하면» «강할수록» 좋다**」")
    P("")
    P("⇒ ★★★ **원전이 준 것은 «방향»이다 — 「가까울수록」·「강할수록」·「바람직」**")
    P("⇒ ⇒ **「−5%면 만점」·「합격선 위 10점」·「+10%」·「+5%」·「+70%p」라는 «수»는**")
    P("     **원전 «어디»에도 «없다** ⇒ 🔴 **Ⓓ4** · 그 주석이 «덮는» 값이 **여덟**이다")
    P("")
    P("★ **이게 「Ⓓ4 가 «고칠 수» 있는 칸」의 «본보기»다** — 고치는 법이 «분명»하다:")
    P("   **「원전의 «권장선»」 → 「원전은 «방향»만 준다. «수»는 «우리»가 정했다」**")
    P("   ⇒ **한 줄만 고치면 «여덟» 값이 «정직»해진다**(«측정»은 «안» 바뀐다)")
    P(F3)
    P("")
    P("---")
    P("")
    P("# ★★ 4. **뜻밖의 소득 — 「«선언»이 «면하게» 한다」**")
    P("")
    P(F3)
    P("🔎 " + BQ + "power_play.py:3-4" + BQ + " —")
    P("   「개념(깃대·깃발·조용한 출발·거래량 마름)=마크 미너비니.")
    P("    **구체 «수치»·계산규칙 = 이 프로젝트의 «공학적 번역»(«원전» «아님»)**」")
    P("")
    P("🔎 그런데 **«수»를 맞대면 «다르다»**:")
    P("   canon:166  「**100% 이상**을 **8주 «미만»**」 · 「횡보 **3~6주** · 조정폭 **최대 20~25%**」")
    P("   우리        " + BQ + "min_flagpole_gain=90.0" + BQ + " · " + BQ + "max_flagpole_days=70"
      + BQ + "(=14주) · " + BQ + "min/max_flag_days=8/30" + BQ + " · " + BQ + "max_flag_depth=20.0" + BQ)
    P("")
    P("⇒ ★★ **수가 «달라도» Ⓓ4 가 «아니다** — **«주장»을 «안» 했기 때문**이다")
    P("⇒ ⇒ ★★★ **처방이 «여기» 있다**: **Ⓓ4 를 «없애는» 길은 «값»을 고치는 게 «아니라**")
    P("     **«주석»을 고치는 것**이다 — " + BQ + "power_play.py" + BQ + " 가 «이미» 그렇게 했다")
    P(F3)
    P("")
    P("---")
    P("")
    P("# 5. **대칭 판정**")
    P("")
    P(F3)
    P("✅ " + SYM_MORE)
    P("🔴 " + SYM_NONE)
    P("")
    P("## ⇒ **✅ 쪽이다 — Ⓓ4 가 «줄» %d · «값» %d 개 «더» 나왔다**"
      % (n4, sum(r[5] for r in ROWS if "Ⓓ4" in r[4])))
    P("")
    P("**§G1 갱신**")
    P("   `205` 까지            **≥ 48**")
    P("   `206` 이 «더하는» Ⓓ4   **%d**" % sum(r[5] for r in ROWS if "Ⓓ4" in r[4]))
    P("   ──────────────────────────")
    P("   ## ⇒ **≥ %d**" % (48 + sum(r[5] for r in ROWS if "Ⓓ4" in r[4])))
    P("")
    P("⚠️ **Ⓓ5 는 §G1 에 «안» 넣는다** — 「canon 에 «없다»」가 「«근거»가 «없다»」를 «뜻하지» 않는다")
    P(F3)
    P("")
    P("---")
    P("")
    P("# 6. **이 판이 «못» 하는 것**")
    P("")
    P(F3)
    P("⛔ ① **canon 을 «전수»로 읽지 «않았다** — «등급»이 «붙은» %d 줄과 그 둘레만 봤다" % len(tagged))
    P("     ⇒ 등급이 «안» 붙은 줄에 «수»가 «더» 있을 수 있다")
    P("⛔ ② **Ⓓ5 의 「없다」는 «내» grep 이 만든 것**이다 — 낱말을 «다르게» 골랐으면 «달랐을» 수 있다")
    P("     🔎 쓴 낱말: 연속·세금·회전·8일·7일·상승일 (" + BQ + "grep -n" + BQ + ")")
    P("⛔ ③ **오닐 주장은 «여전히» 검산 불가** — canon 은 «미너비니» 문서다(" + BQ + "criteria.py:246"
      + BQ + " · " + BQ + "criteria_i.py:361,386,443" + BQ + " 「책 기준」)")
    P("⛔ ④ **Ⓓ2/Ⓓ3 는 «세지» 않았다**(검증 지시) — 그래서 §G1 의 「≥」는 «그대로»다")
    P(F3)
    P("")
    P("---")
    P("")
    P("# 7. **관문 다섯**")
    P("")
    ok = [("① 「몇 중 몇」 형식 — 줄 합 %d = %d" % (n4 + n5 + nx + nok, len(ROWS)),
           n4 + n5 + nx + nok == len(ROWS)),
          ("② 대칭 «둘 다» 인쇄", bool(SYM_MORE) and bool(SYM_NONE)),
          ("③ «훑은» 것을 «먼저» 적었다(§0 공개)", True),
          ("④ canon «등급»을 «우리»가 «다시» 매기지 «않았다» — 판정 칸에 등급 낱말 %d 개"
           % sum(1 for r in ROWS if ("1차-" in r[4] or "3차-" in r[4])),
           not any(("1차-" in r[4] or "3차-" in r[4]) for r in ROWS)),
          ("⑤ canon 근거에 «줄»·«절» 인용 — 없는 행 %d"
           % sum(1 for r in ROWS if not ("canon:" in r[3] or "canon §" in r[3]
                                         or "«안» 다룬다" in r[3])),
           all(("canon:" in r[3] or "canon §" in r[3] or "«안» 다룬다" in r[3]) for r in ROWS))]
    P(F3)
    for nm, v in ok:
        P("%s %s" % ("✅" if v else "🚨", nm))
    P(F3)
    P("")
    P("---")
    P("")
    P("# ⇒ **한 문장**")
    P("")
    P("> ## **Ⓓ4 가 «여덟» 값 «더» 나왔고 — «전부» «한» 주석에서 나왔다.**")
    P("> ## **그리고 «고치는» 법은 «값»이 «아니라» «주석»이다 — " + BQ + "power_play.py" + BQ
      + " 가 «이미» 보여 준다.**")
    P("")
    P("⛔ **커밋은 그쪽 몫입니다.**")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
