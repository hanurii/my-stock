# 255c 덧붙임 — 새 Δ 에서 기본 CI 가 칸 2 로 간다

> 조사 세션 2026-09-10 · 이 파일은 손으로 썼다.
> `255c-threshold-arms.md` 는 각본 출력 그 자체이므로 덮지 않고 따로 둔다(유형 48).
> 낸 각본: `research/handoff/scripts/261-delta-reprice.py`

## 무엇이 달라졌나

`261` 이 Δ 를 현행 규칙(+30/−10)으로 다시 냈다. −1.2263%p 에서 +0.0882%p 로 바뀌었다.
그 자로 오늘 판정 32개를 다시 매기니 하나가 뒤집혔고, 그것이 이 판이다.

| | 값 |
|---|---|
| 무엇 | 문턱으로 갈 수 있는 제일 먼 곳 − 현행 · 블록 20~40 |
| 점추정 | −3.448%p |
| 백분위 CI | [−6.762, +4.583] → 칸 5 (안 바뀜) |
| 기본 CI | [−11.478, −0.133] → 칸 4b 에서 **칸 2** 로 |
| 출처 | `255c-threshold-arms.md:71` |

까닭은 산술이다. 상한 −0.133 이 −1.23 보다 위라 옛 자로는 안 닫혔는데,
−0.0882 보다는 아래라 새 자로는 닫힌다.

## 판정은 안 바뀐다

자료는 하나도 안 바뀌었다. 자만 바뀌었다.

그리고 같은 판의 백분위 CI 는 칸 5 로 그대로다. CI 종류끼리 반대이므로
`232b` 규약대로 답은 **못 가린다** 이고, 이 덧붙임은 그것을 바꾸지 않는다.

⛔ 「문턱이 해롭다가 닫혔다」로 쓸 수 없다.
⛔ 이것을 발견으로 읽으면 안 된다. Δ 를 관측한 격차에서 뽑았기 때문에 생긴 일이고,
   격차가 0 에 가까워질수록 모든 것이 유의해진다. 이 뒤집힘이 그 실증이다.

커밋은 두뇌 몫입니다.
