# 215 — **비용과 «자» — «돌리기» «전»에**

> 조사 세션 · `research/handoff/scripts/215-fa-cost-and-meter.py` · **문서는 이 출력 그 자체**(유형 48)
> ⛔ **이 판은 «안» 돌렸다.** «비용»과 «자»에 **«답»만** 한다.

---

# ★★★ 0. 🚨 **먼저 — 「«잴» 대상」이 «내»가 적은 것과 «다르다**

```
🔎 `grep -rln 'C_QUARTERLY_EPS_MIN|A_ANNUAL_EPS_MIN' scripts/ research/handoff/scripts/`
   → `canslim_lib/criteria.py`(정의) · `screen_canslim.py`(실전 스크리너)
     · `204`·`205`·`214`(우리 «장부» 문서)

## ⇒ 🚨🚨 **«백테스트»는 `criteria.py` 의 EPS 문턱을 «아예» «안» 쓴다**

   ⇒ ⛔ 그러니 **「원전 «4분기 가속» vs 우리 «+25% 수준»」을 «백테스트»로 «잴» 수 «없다**
     — **그 값이 백테스트에 «안» 들어가기 때문**이다
   ⇒ ★★ 두뇌 지시 ②(「`19,24` 에«만» «안» 걸려 있다」)를 **«더» 좁힌다**:
     **「`criteria.py` 의 EPS 문턱은 «실전 스크리너»(`screen_canslim.py`) 축이고 — «백테스트» 축이 «아니다」**
```

## ✅ **그러면 «백테스트»의 펀더 관문은 «무엇»인가**

```
🔎 `201d-rs-threshold.py:123,130,133` (= `208` 도 «같다»)
     `fund, ixf = f92a.load()`
     `a = f92a.asof(arq, p['entry_date'])`
     `v = ... else r103.judge(arq, arq.index(a), ix, 1, 2)`

🔎 `103-code33-strength.py:47-59` `def judge(arq, j, ix, nq, nitem)`
     `for q in range(j, j - nq, -1):`  ⟵ **nq = «몇» 분기를 «볼까»**
     `e0, e1 = _yoy(eps[q], eps[q-4]), _yoy(eps[q-1], eps[q-5])`  ⟵ **YoY «가속»**

## ⇒ ★★★ **우리 백테스트 펀더 관문 = 「«1» 분기 · eps·revenue «둘 다» «가속»」**
   (호출이 `nq = 1` · `nitem = 2`)

⇒ ⇒ ★★ **원전(줄 40 · Ⓜ)은 「최근 «4분기» 동안 «가속»」이다**
   ⇒ ✅ **「«수준» vs «가속»」이 «아니라» — 「«몇» 분기를 «보는가»」다**
   ⇒ ✅ **잴 수 있는 것은 `nq: 1 → 4` «하나»** — **«훨씬» 좁고 «분명»하다**
```

---

# 1. 💰 **답 ① — 비용: «싸다»**

```
## ❓ **펀더 관문이 «스캔» 때 «걸리나**

🔎 `grep -n 'criteria|fundamental|EPS|ARQ|f92a|Code33' backtest_volatility_pilot_us.py`
   → **«한» 줄뿐** — `backtest_volatility_pilot_us.py:215` `fails = {k for k, v in result['criteria'].items() ...}`
   🔎 그 `result['criteria']` 는 `is_gate_near()` 안이고 —
     `evaluate_trend_template()` 이 낸 **«트렌드템플릿» 8조건**이다. **«펀더»가 «아니다**

## ⇒ ✅ **«스캔»엔 펀더가 «없다** ⇒ **경로 «재생성»이 «필요» «없다**

   ⇒ ⛔ `MIN_TURNOVER_EOK`·「200일선 관문」과 **«다르다**
     (그 둘은 `backtest_volatility_pilot_us.py:374` `evaluate_trend_template(...)` 로 **«스캔» 때 걸려** — **216분**)

## 💰 **어림 — «잰» 수로만**

   `201d` 실측: build_pairs «두» 팔 **125초** · 부트스트랩 **97초**
   ⇒ **이 판도 «같은» 몸** ⇒ **≈ 4 분**
   ⚠️ **어림이다** — `nq=4` 는 `arq` 를 «더» 거슬러 보므로 **«더» 많은 진입이 `None`(판정 불가)로 «빠질» 수 있다 ⇒ «팔»이 «작아진다**
```

---

# 2. 📏 **답 ② — 자: «어림»을 «내지» «않는다**

```
🚨 두뇌: 「어림으로는 Δ(1.23)의 **«일곱~열» 배**」 ⇒ **「재도 «못» 가린다」가 «미리» «보인다**」

## ⇒ ⛔ **그 어림을 «쓰지» «않는다** — **«단가» 옮기기(유형 68)**다

   🔎 `201d` MDE **9.040** · `208` MDE **12.783** — **«둘»이 «1.41배» 다르다**
   ⇒ ★ **«두» 판도 «서로» 1.41배인데 — «셋째» 판을 «어림»할 «자»가 «없다**

## ✅ **대신 `208` 방식**: **«효과»를 «안» 보고 **SD · MDE · N «만»** 낸다
   ⇒ ⛔ **각본에 「점추정을 «찍는» 줄」을 «아예» «넣지» 않는다**(금지를 «구조»로)
   ⇒ ⛔ **MDE 가 «커도» 「그래서 «못» 잰다」로 «닫고» «돌리지» «않는다**(두뇌 관문 ③)
```

---

# 3. ⛔ **«미리» 박을 것**(두뇌 지시 «그대로»)

```
⛔ ① **「Ⓜ 이라서 «잰다»」가 «아니라 「Ⓜ 이고 «잴» 수 «있어서»」**다. **Ⓤ «여덟»은 «못» 잰다**
⛔ ② **좁게 적는다** — 「`criteria.py:19,24` 가 «수준»이다」가 «아니라**
     **「`criteria.py` 는 «백테스트»가 «안» 쓴다. 백테스트 펀더는 `103.judge(nq=1)` 다」**
⛔ ③ **MDE 가 «커도» «닫지» 않는다**(`201d` 3.9시간의 «교훈»)
⛔ ④ 🚨 **«이겨도» «못» 쓰는 것**: `nq=4` 가 «나아도» 그건 **「«백테스트» 관문을 바꾸라」**이지
     **「원전이 «옳다»」가 «아니다** — 원전은 **«4분기»**를 말했고 우리 `nq` 는 **«분기 수»**이나, **「가속」의 «정의»가 «같은지»는 «안» 쟀다**
```

---

# ⇒ **한 문장**

> ## **비용은 «싸다**(펀더는 «스캔»에 «없다· ≈4분) — 그러나 **«잴» 것이 «바뀌었다**.
> ## **`criteria.py` 의 «+25% 수준»은 «백테스트»가 «안» 쓴다.**
> ## **«진짜» 대응은 `103.judge` 의 **`nq: 1 → 4`** «하나»다.**

⛔ **«돌릴지»는 그쪽 «결정»입니다. 저는 «답»만 했습니다.**
⛔ **커밋은 그쪽 몫입니다.**
