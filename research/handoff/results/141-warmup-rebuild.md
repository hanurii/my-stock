# 141 — 웜업 재빌드 · **자료만 만들었다. 특징·체·성적은 «없다»**

**의뢰** 두뇌 세션(140번 「경로에 진입 «전»이 없다」의 후속) · **구현** 조사 세션
**산출** `D:/stock-data/uspath-warm/full/uspath_YYYY.json` (28개 · **4.86 GB**)
**코드** `scripts/backtest_volatility_pilot_us.py`(옵트인 `--emit-warmup`·`--warm-days`) ·
`research/handoff/scripts/141-warmup-gates.py` · `141b`·`141c`·`141d`·`141e`·`141f` · `entry_envelope.py`

> ## 🚨 자료 스냅숏 — **모든 숫자에 붙는다**
> **2026-09-01 재빌드 · 시세 ~2026-08-26 · 원천 Sharadar 2026-08-27 내려받음 · warm 700 · W 400**

---

# 1. 무엇을 덧붙였나

```
그대로 둠   d · o · h · l · c        ← **d[0] == entry_date 불변** (저장소 전부가 이걸 전제한다)
새로 붙임   pre_d · pre_o · pre_h · pre_l · pre_c · pre_v   (진입 «전» 최대 400봉)
            pre_n                                          (실제로 몇 봉 붙었나)
            v                                              (진입 «후» 거래량 — **청산 전용**)
기본값      `--emit-warmup` 을 «안» 주면 옛 동작과 동일
```

## 🚨 자료를 «구하지» 않았다 — **들고 있는데 버리고 있었다**
`backtest_volatility_pilot_us.py` 가 경로를 낼 때 `s["dates"][ni:_e]` 로 **진입일부터** 잘랐고
`s["volumes"]` 는 **아예 안 썼다**. 200일선으로 후보를 거르므로 **진입 «전» 250봉 이상은 반드시 메모리에 있었다.**

## `v` 를 넣은 이유 (한 번 뒤집힌 결정)
```
처음 안   「돌파일 거래량은 장 전 예약 시점에 «없는» 값이니 빼자」
뒤집은 것 **진입과 청산은 사정이 다르다** — 청산은 «종가»를 보고 판단하므로 그날 거래량을 «안다».
          저장소가 이미 청산에서 거래량을 쓴다(보유점검 규칙 ③⑥ · 청산부담 ADV20 · 원전 「50일선 이탈」)
결론      **「없애서 막기」 대신 「안 넘겨서 막기」** — 봉투(`entry_envelope.py`)가 막는다
```

---

# 2. ★ 봉투 — **진입 시점에 «없는» 값은 «죽는다»**

`research/handoff/scripts/entry_envelope.py` · 자체시험 **9/9 통과**
```
진입 뷰   pre_* 와 진입 기록만. 경로(d·o·h·l·c·v)는 «한 칸도» 안 준다
청산 뷰   경로를 주되 **[0] 칸(진입일)을 가린다** — v[0]·c[0] 을 읽으면 IndexError
차단      .get() · setdefault · pop 우회까지
```
🚨 **가장 중요한 시험은 ①이다** — **봉투 «없이»는 커닝이 «된다»**(값 1.1500 이 나온다).
그게 없으면 나머지 여덟이 「아무것도 안 하는 코드」일 수 있다. ⑤(정직한 특징은 «살아 있다」)도 같이 건다.

---

# 3. 관문 아홉 — **전부 통과** (`_141g9.log`)

| | 무엇 | 결과 |
|---|---|---|
| ㉠′ | 옛 경로 vs 새 경로 — 공유 키 12개 | **통과** (아래 §4) |
| ㉡′ | `pre_d[-1] == scan_date` | **0 / 145,282** |
| ㉢ | 웜업 길이 400봉 미만 | **6,962건 (4.79%)** — 세기만 함 |
| ㉣ | 키 구성 · 길이 정합 | **0** |
| ㉤ | 원본 시계열의 «불연속» | **1건** (단독) |
| ㉥ | **돌연변이 — 관문이 «실패할 수 있는가»** | §5 |
| ㉦ | 배열 «순서» | 어긋난 해 {2021, 2023, 2026} = 기대와 일치 |
| ㉧ | 연도별 후보 «수» | 기대와 일치 |
| ㉨ | **`pre_*` 값 대조** | **5,847,715쌍 · 어긋남 0** |

---

# 4. ★★ ㉠′ — **「예외로 봐준다」가 아니라 «건수를 박았다»**

```
일치 140,400 · 자람 4,805 · 재조정 22 · 자람+재조정 2 · 꼬리수정 53 · **그 밖 0**
옛쪽만 5 · 새쪽만 19
꼬리수정 깊이 {0: 142,653 · 1: 901 · 2: 4 · 3: 8 · 4: 18 · **5: 1,692** · 250: 6}
```
**모든 차이가 네 모양으로 설명되고, 건수가 하나만 늘어도 잡힌다.** 문턱이 아니라 «회귀 기준선»이다.

## 🚨 ★ 새로 알아낸 사실 — **스냅숏의 «마지막 주»가 잠정치다**
```
옛 스냅숏의 **마지막 5봉 이내**가 사후에 «고쳐져» 있다
수정이 «시작된» 날짜가 **2026-08-17 (2,162건) · 08-21 (1,007건)** 에 몰린다
```
> ### 저장소가 알던 것: 「다시 받으면 과거가 바뀐다 — **소급 분할 조정·티커 개명**」
> ### 새로 안 것: **「마지막 ~5거래일의 «값» 자체가 잠정이다」** — **다른 사실**이다.
> ### 백테스트 결론에는 거의 영향이 없다(27년 중 끝 며칠). **실거래 설계에는 넣어야 한다.**

## 나머지 세 모양
```
자람      옛 배열이 새 배열의 «앞부분». 시세 마지막이 08-21 → 08-26 으로 자람(대부분 +3봉)
재조정    날짜는 그대로, 가격만 «한 배수»(예: IESC 정확히 2.0배). 소급 분할 반영. 4,832 중 24건
옛/새쪽만 자료 갱신으로 경계에서 후보가 들고 남 (2021·2023·2026)
```

---

# 5. ㉥ 돌연변이 — **관문이 «실패할 수 있는가»**

🚨 **한 해만 돌리면 무의미하다** — 회귀 기준선이 28년 전체 값이라 «손 안 댄 판»부터 미통과가 난다.
그러면 「망가뜨려서 실패」와 「기준선이 안 맞아서 실패」를 **못 가른다.** 그래서 **전 구간판**(`141e`)으로 한다.
(4.86GB 를 복사하지 않으려고 **하드링크 그림자 디렉터리**를 쓴다.)

## ★★ 1차 시험이 **제 관문 둘이 죽어 있는 것**을 잡았다
```
② pre_c[-1] × 1.0001   →  🚨 **못 잡음** — `pre_*` 의 «값»을 아무 관문도 안 보고 있었다
                          ㉠′ 는 SHARED 만 보는데 pre_* 는 **옛 파일에 없어서 애초에 빠져 있었다**
                          → **재빌드의 «존재 이유»가 무검증이었다.** ㉨ 를 새로 만들어 막음
⑥ 배열 순서 뒤집기       →  🚨 **못 잡음** — 건수를 고정하면서 `g_order` 가 `ok` 에 «안» 들어가게 만듦
                          → **관문을 «고치다가» 유형 24′ 를 새로 만들었다.** 고침
```

## 2차 시험 — **여섯 다 잡힌다**
```
⓪ 손 안 댄 판 (진짜 산출 · 전 구간)   **통과**   ← 관문이 «너무 예민한» 것도 아니다
① c[0] × 1.0001                    실패(=잡았다) ✅      ④ d[0] → 1900-01-01   ✅
③ pivot × 1.0001                   ✅                   ⑤ 경로 한 건 지우기     ✅
⑥ 배열 순서 뒤집기                   ✅ (㉦ 되살림 — 어긋난 해 [2015,2021,2023,2026] 로 잡힘)
② pre_c[-1] × 1.0001               ✅ **㉨ 를 넓힌 뒤 잡힘** (AIV 2015-01-02 · 어긋남 1로 미통과)
```
🚨 **①③④⑤⑥ 은 ㉨ 를 넓히기 «전» 판에서 잡혔고, ② 는 넓힌 «뒤» 따로 던져 잡았다.**
   여섯을 «한 실행»에서 같이 돌리지는 않았다 — 다만 ㉨ 확장은 «더 예민해지는» 방향뿐이라
   ①③④⑤⑥ 의 판정은 그대로 선다. **그 사실을 그대로 적어 둔다.**

## 🚨 ㉨ 의 «덮는 범위» — 한계를 숫자로 적는다
```
pre 봉 **57,672,531**개 중 **46,398,929개 (80.5%)** 를 겹쳐서 대조 · **어긋남 0**
덮지 못한 **19.5%** = 겹칠 상대가 없는 봉
```
→ **체계적 오류(잘못된 오프셋·잘못된 배열)는 4,640만 쌍이 잡는다.**
→ **«한 봉만» 망가진 경우는 19.5% 안에 있으면 못 잡는다.** 처음 만든 판은 덮는 범위가 **10%**뿐이었다.

---

# 6. 사고 기록 — **제가 낸 것 셋**

```
① `parents[2]` / `[3]`   몰이꾼의 뿌리 경로를 틀려 17년이 즉시 실패.
                         🚨 **재개 구조가 받아 냈다** — 기존 11년 무손상, 실패 연도는 파일을 «안» 만듦.
                         계획한 시험이 아니라 «사고가 시험해 준 것»이지만 증거로는 같다
② **창을 틀림 (2017·2021)** 「각 파일 params 에서 인자를 읽어 맞췄다」고 «적어 놓고»
                         1999·2026 양 끝만 확인하고 나머지를 「연초~연말」로 «가정»했다
                         옛: 2017-09-05 · 2021-02-01.  → 다시 지었고 겹침 대조 **필드 어긋남 0**
                         ★ **「읽었다」고 적는 것과 «전 연도를 대조하는» 것은 다르다**
③ ㉦ 무력화              ⑥ 그것. 관문을 고치다가 새 결함을 만듦
④ 서식 인자 개수         `%` 인자 하나 모자라 관문이 마지막에 죽음 (133 교훈 자리)
```

# 7. 실행 방식 — **긴 작업은 «분리»해서 띄운다**
```
관측   도구에 맡긴 백그라운드가 **6판 중 4판** 거둬짐 (1~2분짜리도 55분짜리도 · 길이로 «안» 갈림)
★ 가장 단단한 한 줄:  **대기자가 거둬진 «그 시각»에 분리 실행은 «살아 있었다»**
🚨 기전  **모른다.** Windows 로그가 0건이라 「OS 가 죽인 게 아니다」까지만 말할 수 있다
처방   `Start-Process` 분리 + 로그 폴링 · **재개 구조를 «같이»** · 성공만 보지 말고 **실패·예외도 잡기**
```

# 8. 다음 판에 넘기는 것 (여기서 «정하지» 않는다)
```
· 400봉 미만 6,962건(4.79%)을 어찌할까 — **빼지 않는다**는 것만 확정. 특징 수준 처리는 다음 판
  (`pre_n` 을 «층 나누기»에 쓰는 안)
· ㉤ 1건(2005 HYDGQ) — `liveness.py` 의 「5일 «연속» 거래량 0」이 이 건을 못 잡는다(연속 2일).
  「최근 N일 중 0인 날의 «비율»」이 나을 수 있으나 **지금 정하지 않는다**
· 문턱은 **새 자료에서 다시** 계산한다 (평균 = 0.05 × 매수)
· 사전등록 첫 줄에 **「특징 함수는 `pre_*` 만 받는다 · `v` 는 안 넘긴다」**
· 검출기는 **돌파일 거래량 조건을 «끈» 판**으로 (완화가 아니라 «정확화»)
```
