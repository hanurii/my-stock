# 233a - **«전» 갈무리**(관문 ①) + **«눈»으로 확인**(관문 ②)

> 조사 세션 · `research/handoff/scripts/233a-snapshot.py` · **문서는 이 출력 그 자체**(유형 48)
> ⛔ **«아무것»도 «고치지» 않았다** · 여기 MDE·CI 는 **「«전»/«후» 나란히」 자리에서«만** 쓴다

---

# 1. **관문 ② — «눈»으로 «읽어» «확인**(불일치 14 «중» **3** 각본)

```
   184-swap-data-axis.py    :486  ✅ «맞다»
        자리 = 진입일«만»    all_d = sorted({z[0] for z in folded})
   184-swap-data-axis.py    :441  ✅ «맞다»
        보유 = 「거래일」     hold = d.index(rd) if (rd and rd in d) else len(d) - 1
   229-roe-threshold.py     :179  ✅ «맞다»
        자리 = 진입일«만»    all_d = sorted({d for lst in built.values() for d, _h, _n in lst})
   229-roe-threshold.py     :98   ✅ «맞다»
        보유 = 「거래일」     hold = d.index(rd) if (rd and rd in d) else len(d) - 1
   227b-noliq-judge.py      :216  ✅ «맞다»
        자리 = 진입일«만»    all_d = sorted({d for lst in built.values() for d, _h, _n in lst})
   227b-noliq-judge.py      :138  ✅ «맞다»
        보유 = 「거래일」     hold = d.index(rd) if (rd and rd in d) else len(d) - 1

   ★ **6 중 6** 이 «내»가 «적은» 것과 «같다** ⇒ ✅ **정규식 분류가 «맞는다**(3 각본 표본)
   ⛔ **3 각본 «표본»이다** — **14 «전부»를 «읽은» 게 «아니다**
```

---

# 2. **`201d` 의 「지금 «수»」 — «전** (⛔ **«고치기» «전»에 «박는다**)

```
   출처 `results/201d-rs-threshold.md` · 캐시 `.cache/bt5y/out/201d-arms.json`

   **날짜 자리 수 = 3,723**   ⟵  `len(sorted(set(entry_dates)))`
   **  ①      거래 수 = 5,951**   ⟵  `len(built[k])`
   **  ℝ      거래 수 = 7,680**   ⟵  `len(built[k])`
   **  ①VCP   거래 수 = 4,376**   ⟵  `len(built[k])`
   **  ℝVCP   거래 수 = 5,718**   ⟵  `len(built[k])`
   **  ①3C    거래 수 = 1,395**   ⟵  `len(built[k])`
   **  ℝ3C    거래 수 = 1,759**   ⟵  `len(built[k])`
   **  ①PP    거래 수 = 180**   ⟵  `len(built[k])`
   **  ℝPP    거래 수 = 203**   ⟵  `len(built[k])`
```

| 짝 | 점추정 | 95% CI | CI폭 | MDE | 판정칸 |
|---|---:|---:|---:|---:|:--|
| **ℝ−①** | **+2.943%p** | [-5.723, +7.052] | 12.77 | 9.040 | **5** 🚨 «못 가린다» |
| 🔎묘사 **VCP** | **+4.018%p** | [-5.808, +7.033] | 12.84 | 8.935 | **5** 🚨 «못 가린다» |
| 🔎묘사 **3C** | **+0.847%p** | [-5.127, +4.308] | 9.44 | 6.688 | **5** 🚨 «못 가린다» |
| 🔎묘사 **PP** | **+0.572%p** | [-2.062, +1.721] | 3.78 | 2.642 | **5** 🚨 «못 가린다» |
| **ℝ−①** | **+2.943%p** | [-5.591, +6.264] | 11.86 | 8.515 | **5** 🚨 «못 가린다» |
| 🔎묘사 **VCP** | **+4.018%p** | [-4.861, +7.278] | 12.14 | 8.837 | **5** 🚨 «못 가린다» |
| 🔎묘사 **3C** | **+0.847%p** | [-5.025, +4.180] | 9.21 | 6.377 | **5** 🚨 «못 가린다» |
| 🔎묘사 **PP** | **+0.572%p** | [-1.742, +2.115] | 3.86 | 2.797 | **5** 🚨 «못 가린다» |

```
   찍은 행 **8** (블록 «둘» x 팔 «넷»)
   ★ **판정 짝은 ℝ−① «하나»** — 나머지 셋은 **«묘사»**다(`201d` 사전등록 ②)
   ⛔ **`233` 은 «판정» 짝«만** 다시 «잰다** — **묘사 셋은 «안** 잰다
```

---

# ⚠️ **이 갈무리가 «못» 하는 것**

```
⛔ ① **3 각본만 «눈»으로 봤다** — 나머지 11 은 **정규식 «분류»뿐**
⛔ ② 🚨 **`232d` 가 `184` 의 «자리» 줄을 **:495**(=cnt 「라벨」)로 «찍었다** —
     **«진짜» 구성은 :486** 이다. **«분류»는 «맞고» «줄 번호»가 «라벨»을 가리켰다**
     ⇒ ★ **「«눈»으로 읽어라」가 «값»을 했다**(관문 ②)
⛔ ③ **여기 수는 「«전»/«후» 나란히」 «자리»에서«만** 쓴다 — **«인용» 금지가 «산다**
⛔ ④ **원본 `201d-rs-threshold.py`·`201d-arms.json` 은 «건드리지» 않았다** — 그것이 «진짜» 갈무리다
```

⛔ **커밋은 두뇌 몫입니다.**
