# 21 — 출처 감사 (메모리 12건)

## 🚨 자료 기준일 — **2026-08-20** (한국 pdata)

> **이 문서의 한국 숫자는 `price_20260820.json` 까지의 자료로 나왔다.**
> **추정이 아니라 복원값이다** — 산출 코드 `research/handoff/scripts/_asof_recover.py`.
> 이 파일의 **마지막 커밋 시각**에 pdata 에 **이미 있던** 가장 늦은 거래일을 계산했다.
> **결과 문서 39개 전부 `2026-08-20` · 미상 0개.**
>
> ★ 앞끝이 **2026-08-21 16:09 부터 2026-08-24 20:00 까지 줄곧 `2026-08-20`** 이었고
> 모든 커밋이 그 사이에 있다 → **그 안에서 언제 계산했든 답이 같다.**
> **상한이 아니라 확정에 가깝다.**
>
> 🚨 **창 라벨 「~ 2026-08-21」은 «명령줄에 지정한 값»이지 자료 끝이 아니다.**
> `price_20260821.json` 은 **2026-08-24 20:00 에야 들어왔다.**
> 창 끝으로 라벨했다면 **자료가 없던 날짜를 자료 기준일로 적을 뻔했다.**
> **실제 마지막 거래일은 `2026-08-20`.**
>
> ⚠️ **pdata 는 매 영업일 자란다** — 다시 돌리면 마지막 해의 거래일 수와 값이 조금 다르다.
> **재현 실패가 아니다.** 미국(Sharadar)은 **정적 파일**(`stocks-10Y.csv.zip` · 2026-08-24
> 07:14 고정 · 내려받기 코드 없음)이라 **재현이 고정된다. 두 시장의 재현 성질이 다르다.**


- 지시서 `research/handoff/tasks/21-provenance-audit.md`
- **스크립트 없음.** 이 과제는 읽기·대조뿐이라 산출 스크립트를 만들지 않았다. 재계산은 한 건(② Fisher)뿐이고 아래 본문에 식과 값을 적었다.
- **🚨 안전조항 준수:** 실행한 코드는 **없다**. `pivot_backtest_nextday_multi.py`는 네트워크 호출 0·자기 출력 JSON만 쓰므로 실행 가능하다고 확인했으나 **실행하지 않았다**. DART·Naver를 호출하는 파일은 열지도 않았다.
- 감사 시점 커밋 `ed04f1b9` · 브랜치 `chore/sepa-2026-08-03`

> ### ⚠️ 먼저 — **네 개 라벨로 안 덮이는 칸이 생겼다** (지시서 결함 보고)
> 출처 축의 값은 `코드-값 일치` / `값 불일치` / `코드 없음` / `하드코딩·비계산` 넷이다.
> 그런데 **"코드는 있는데 내가 돌리지 않아 값을 대조하지 못한 경우"**가 있다.
> 이것을 `코드-값 일치`로 적으면 **하지 않은 확인을 했다고 적는 것**이고,
> `코드 없음`으로 적으면 **있는 코드를 없다고 적는 것**이다.
> → 다섯 번째 값 **`재현 미실행`**을 썼다. **임의로 늘린 것이므로 두뇌 세션이 되돌리면 되돌린다.**
>
> 그리고 여러 행에서 **"메모리가 지목한 파일은 있는데, 그 파일이 그 숫자를 만드는 코드가 아니다"**가 나왔다.
> 이 경우 출처는 `코드 없음`(그 숫자를 내는 코드가 없다)으로 적고, **존재하는 파일이 무엇인지 옆에 적었다.**

## 🚨 자산 곡선의 한계 — **슬롯5가 「없는 돈」을 쓴다** (2026-08-24 실측)

> **이 문서에 자산(슬롯5 자산곡선) 숫자가 있으면 이 한계가 걸린다.**
>
> 슬롯 시뮬은 새 칸 크기를 **`자산 ÷ 5`** 로 잡는데, 그 「자산」에는
> **아직 안 판 칸에 묶인 돈**이 들어 있다. 손실이 확정돼 자산이 줄어든 뒤 빈 칸이
> 여러 개 생기면 **실제 현금보다 큰 금액을 배정한다.**
>
> **실측** (`research/handoff/scripts/42-checks.py`):
> 자유 현금(= 자산 − 아직 안 판 몫) **최솟값 −0.143930 = 초기 자본의 14.4%**.
> 🚨 **정본 `slot_sim` 과 분할판이 소수점까지 같은 값이다 — 정본의 성질이다.**
>
> | | |
> |---|---|
> | **거래당 지표** | **안 걸린다** (칸 크기와 무관) |
> | **자산 축** | **걸린다** |
> | 크기 | 잰 값 — 초기 자본의 **14.4%** (변형별 7.1~18.4%) |
> | 방향 | **미정** — 초과는 «손실이 난 뒤»에 생기므로 그 뒤 수익률에 달렸다 |
>
> 41번(1회차)부터는 **현금 제약판**(`칸 = min(자산/5, 가용현금/빈칸)`)을 헤드라인으로 쓴다.
> **이 문서의 자산 숫자는 「정본판」이다.**


## 🚨 거래당의 한계 — **체결가가 «종가»다** (2026-08-24 · **정정판**)

> **이 문서에 한국 거래당 숫자가 있으면 이 괄호가 걸린다.**
>
> 하네스 `canslim_lib/pivot_backtest.py:47` 은 목표·손절 **«가격»이 아니라 그날 «종가»**로
> 결착한다. **3,776건 전부** 그렇다.
> 🚨 **사용자는 손절을 «시장가»로 집행하므로 갭다운이면 선보다 «아래»에서 나간다.**
>
> | 한국 5.6년 거래당 | |
> |---|---|
> | 종가 결착 (하네스) | **−0.0776%** |
> | **실집행 근사** (`max(목표,시가)` / `min(선,시가)`) | **−0.0013%** |
> | **정본** | **`[−0.0776% , −0.0013%]` — 실제는 그 사이** |
>
> **폭 0.0763%p = MDE 의 5%. 판정은 안 바뀐다.**
>
> 🚨 **옛 괄호 `[−0.0786 , +0.053]` 은 철회한다.** 그 «+0.053» 은 **「선 «정확히»에서 체결」**
> 규약이었다 — **−10%에 딱, +20%에 딱.** 사용자는 손절을 시장가로 내므로 **갭다운을 맞는다.**
> **달성 불가한 낙관치였다.**
> **「부호가 바뀐다」도 철회한다** — 실집행 근사는 0에 **닿을 뿐 넘지 않는다.**
>
> ⚠️ **승·패가 반대로 당긴다** — **규약을 맞추니 승 쪽은 한국·미국 부호가 일치한다**
> (한국 **+0.0337%p** · 미국 **+0.019%p**). **패 쪽은 여전히 반대**
> (한국 **+0.0429%p** · 미국 **−0.122%p**) — 가격제한폭 가설을 검정 중이다(44번).
## 12행 두 축 판정표

`코드-값 일치`는 "그 숫자가 옳다"가 아니라 **"그 코드가 그 숫자를 낸다"**이다.
`건전성`의 기본값은 **`미확인`**이다 — `문제 없음`은 적극적으로 보였을 때만 쓴다. **이번에 쓴 행은 없다.**

| # | 메모리 | 감사한 주장 | **출처** | **건전성** |
|---|---|---|---|---|
| ① | `correction-regime-volume-edge` | ≥3배 국면무관 85%승 · 2~3배 상승57/조정37 · OOS 2023-24 | **코드 없음** (지목 파일은 있으나 거래량 코드 부재) | **룩어헤드** |
| ② | `actionable-leading-discriminators` | 돌파일 거래량 1.5배+ 유일 생존 · p≈.018 | **코드 없음** | **룩어헤드** |
| ③ | `sepa-nextday-breakout-findings` | rel-vol≥1.5 → 57(.5)% | **코드-값 일치** ¹ | **룩어헤드** |
| ③ˣ | 〃 (색인줄만) | 정확스택 **57.4%** | **값 불일치** (저장소에 57.4 없음) | 미확인 |
| ④ | `minervini-breakout-validation` | 2,446진입 · 더블율 18→40% · 실현 +5.3%/MFE +92% | **코드 없음** | **미확인** |
| ⑤ | `winner-characteristics` | RS 판별자 · 903레코드 구간별 승률 | **코드 없음** (`cmp_exit.py` 부재) | **미확인** |
| ⑥ | `scorecard-winner-traits` | VCP 47% vs 무셋업 10% · 상승국면 64/29 | **코드 없음** | **미확인** |
| ⑦ | `threshold-newcomer-caution` | 신참 +0.78%p (p=.003) | **재현 미실행** (코드 존재·**git 미추적**) | **미확인** |
| ⑧ | `correction-bottom-leader-factors` | 9바닥 21,026관측 · 2단계 +5.4%p | **코드 없음** | **미확인** |
| ⑨ | `gate-relaxation-backtest-verdict` | 같은날 p=.332/.902/.749 · ⑦완화 117건 38.5% | **코드 없음** (하네스만 존재) | **미확인** |
| ⑩ | `ipo-track-validation` | 위너 92%(22/24) · 91%가 4일 내 손절 | **코드 없음** (문서·운영 스크리너만 존재) | **미확인** |
| ⑪ | `volatility-pilot-backtest` | 상승 +2.68%/거래 vs 조정 −1.02%/거래 | **코드 없음** (하네스에 **국면 축이 없다**) | **미확인** |
| ⑫ | `liquidity-burden-display` | 34건 실측 · 손절일 = 평소 **1.02배** | **하드코딩·비계산** (주석 문자열) | **미확인** |

¹ 필드 정의와 57.5%는 대조했다. **하네스를 돌려 재현하지는 않았다**(③의 재현 항목 참조).

**`문제 없음` 0행 · `룩어헤드` 3행 · `코드 없음` 8행 · `재현 미실행` 1행 · `값 불일치` 1행 · `하드코딩·비계산` 1행.**

---

## ①②③ — **독립으로 읽은 뒤 나란히 놓은 사실**

지시대로 ㉠셋을 서로 안 보고 각각 읽고 → ㉡사실만 대조했다. **㉢어긋남의 "원인"은 쓰지 않는다.**

### ㉠ 각각 읽은 것

**공통 함수** `scripts/canslim_lib/pivot_backtest.py:18-25`

```python
def rel_volume(series, idx, window=50):
    """idx일 거래량 ÷ 직전 window 거래일 평균(idx 제외). 표본/데이터 없으면 None."""
```

| | ① correction-regime | ② actionable-leading | ③ sepa-nextday |
|---|---|---|---|
| **거래량 필드 이름** | `rel_vol` / `rel_vol_bucket` | "돌파일 상대거래량" (필드명 미기재) | `rel_vol` / `rel_vol_bucket` |
| **인덱스** | 코드 미발견 | 코드 미발견 | **`rel_volume(full, ni)`** — `ni = dates.index(asof)+1` = **진입일=돌파일** (`pivot_backtest_nextday.py:110,125`) |
| **기간** | 2025 전체 + OOS 2023-24 | 2026-06-29 ~ 08-15 | 2025-10 ~ 2026-06 (9개월) |
| **n** | **2,427** (상승 1,658 · 조정 769) | 이벤트 40 · 진입 25 · **확정 19** | **3,487** |
| **필터** | 국면 2분할 × 거래량 4버킷 | 진입임박 ∧ 주도섹터 태그 181종목 | 중복제거(보유 중 재매수 금지) |
| **문턱값** | ≥1.5 / ≥2 / ≥3 | ≥1.5 (경계 1.4 이동 검사) | ≥1.5 |
| **지목 산출물** | `pivot_backtest_nextday_multi.py` | **없음** | `pivot_backtest_nextday{,_multi}.py`, `resolve_ambiguous_multi.py`, `public/data/pivot-backtest-nextday-*.json`, 리포트 HTML (커밋 `ec262ad`) |

### ㉡ 나란히 놓은 사실

1. **①이 지목한 `scripts/pivot_backtest_nextday_multi.py`(165행, `ec262adb`)에는 거래량 코드가 없다.** `events.append({...})`에 `rel_vol` 키가 없고, 파일 전체에 `vol` 문자열이 **0회** 나온다.
2. **저장소에서 `rel_vol`/`rel_vol_bucket`을 만드는 파일은 두 개뿐이다** — `scripts/pivot_backtest.py:108-110`(인덱스 `bi`)과 `scripts/pivot_backtest_nextday.py:125-127`(인덱스 `ni`). 둘 다 **진입일 인덱스**를 넘긴다.
3. **`2427` · `1658` · `769`는 저장소의 어떤 `.py`·`.md`에도 없다.**
4. **②는 자신의 수치를 ③에 귀속시킨다** — 본문: "`[[sepa-nextday-breakout-findings]]` **2427건** rel-vol≥1.5→57%와 합치". 그런데 **2,427건은 ①의 표본**이고 ③의 표본은 **3,487건**이다.
5. **"57%"는 ③ 본문·리포트에서 57.5%다** (`docs/research/2026-07-07-sepa-nextday-report.html:137` — "38% → **57.5%**, 거래당 +3.63%").
6. **②의 ≥1.5 팔은 3건이다** (2승 1패). Fisher 초기하 확률 재계산 `C(3,2)·C(16,0)/C(19,2)` = **0.01754** — 메모리의 "p≈0.018"과 일치.
7. **①의 본문은 스스로 룩어헤드를 적고 있다** (33·35·37행): "위 모든 셀 승률·EV·청산결과는 **진입일 '종일' 거래량**으로 게이트/버킷했는데, 이는 **살 때 모르는 값(룩어헤드)**", "**데일리 백테스트의 거래량 엣지는 사실상 전부 룩어헤드였음**", 분봉 1,785건 재검증 "장중페이스 버킷 승률 47/41/56/52/51% = **페이스 무관 평평**", 8개월 3,314건 통합 "상승장 동시간대≥3배 = **56%(n170)** vs 기준선 51%".
8. **①의 `description:`(3행)과 `MEMORY.md` 색인줄(74행)에는 그 단서가 없다.** 둘 다 "**≥3배면 국면 무관 85%승**"을 조건 없이 싣는다.
9. **③ 본문(15행)은 필드 정의를 정확히 적는다** — "돌파당일 rel-vol(당일량/50일평균)". **룩어헤드라는 말은 ③ 본문·색인줄 어디에도 없다.**
10. **②의 본문에서 '룩어헤드'는 국면값에만 붙는다** — "당일 국면값은 룩어헤드". 거래량 판별자에는 붙지 않는다.
11. **`docs/research/2026-07-08-correction-regime-findings.md:100-102`가 같은 사실을 이미 문서화하고 있다** — 실시간판 ~56% vs 종일판 71~81%.
12. **③ 색인줄의 "정확스택(등가중상승+거래량+15/-7.5)=57.4%"** — `57.4`는 `docs/`·`public/data/pivot-backtest-*`·`src/` 어디에도 없다.

---

## 여섯 물음 — 12건 각각

각 항목: (1) 코드 존재/위치·커밋 (2) 하드코딩 vs 계산 (3) 표본 정의 (4) 룩어헤드 (5) 다중검정 (6) 재현.

### ① `correction-regime-volume-edge`
1. **없음.** 지목된 `scripts/pivot_backtest_nextday_multi.py`(`ec262adb`, 165행)는 존재하나 거래량 코드가 없다. 2,427건 국면×거래량 표를 내는 파일은 저장소에 없다. 근거 문서는 `docs/research/2026-07-08-correction-regime-findings.md`(존재).
2. 대조할 코드가 없어 **판별 불가**.
3. 메모리 "2025 전체 2427거래(상승1658·조정769)" — **대조할 산출물 없음**.
4. **있음.** 본문 33행이 스스로 명시. 필드 = 진입일 종일 거래량.
5. 국면 2 × 버킷 4 × 손절폭·목표 격자를 훑었으나 **보정 보고 없음**. 단 본문 28행이 일부를 "과적합으로 판명(인샘플 전용)"으로 적고 OOS 결과를 나눠 싣는다.
6. **미실행** (코드 없음).

### ② `actionable-leading-discriminators`
1. **없음.** 본문·`description` 어디에도 경로가 없고, 저장소 스캔에서 해당 산출물을 찾지 못했다.
2. 판별 불가.
3. "6/29~8/15 git 야간커밋 32개 · 주도섹터 태그 181종목 · 이벤트 40 · 진입 25 · **확정 19**". **≥1.5 팔의 n = 3.**
4. **있음.** "돌파일 상대거래량" = 진입일 종일 거래량. 본문은 룩어헤드를 **국면값에만** 표기.
5. 본문이 훑은 축: 거래량 · 국면 · RS · 사전상승폭 r63 · 베이스 깊이 · 타이트니스 · 수축횟수 · 섹터 라벨 · 피벗 도달속도 = **9축**. **보정 보고 없음.** 본문은 "p는 낙관적(총 승리 2건·진입일 클러스터 상관)"이라고 적는다.
6. Fisher 값만 재계산 — **0.01754** (일치). 원 데이터 재현은 **미실행**.

### ③ `sepa-nextday-breakout-findings`
1. **있음.** `scripts/pivot_backtest_nextday.py:110,125,127` (`ec262adb`) · `scripts/canslim_lib/pivot_backtest.py:18-25` · 산출 JSON 10개 `public/data/pivot-backtest-nextday-*.json` · 리포트 `docs/research/2026-07-07-sepa-nextday-report.html`.
2. **계산.** `rel_volume(full, ni)` → `relvol_bucket()` → 특성별 집계(`:144`).
3. 메모리 "9개월 2025-10~2026-06, 3,487거래(중복제거)" — 코드는 중복제거를 별도 단계로 두므로 **필드 정의는 일치**, 건수는 미대조.
4. **있음.** `ni = dates.index(asof) + 1` = 진입일. 진입은 장중 피벗 돌파인데 그날 거래량은 종가에야 확정된다. 이 세션이 금지한 `rel_vol_entry_LOOKAHEAD_DO_NOT_FILTER`와 **같은 구조**다.
5. 본문이 훑은 축: 거래량 버킷 · 국면(20MA/50MA) · R배수 격자 · 청산 변형 · 추격폭 · 붐빔 · 정적 최소필터 · RS. **보정 보고 없음.**
6. **미실행.** 57.5%는 리포트 HTML 137행과 **문자 일치**로만 확인했다. 하네스 재실행은 하지 않았다.
   **색인줄의 57.4%는 저장소에서 찾지 못했다.**

### ④ `minervini-breakout-validation`
1. **없음.** 본문이 지목하는 `scripts/screen_buy_recommendations.py`(`1ea08eb4`, 200행)는 **운영용 매수 추천 스크리너**로, `canslim_lib/superperf.py`의 점수를 소비한다(`:143-145`). "2,446진입 2022~26 백테스트"를 내는 파일은 없다.
2. 판별 불가.
3. "2446진입 2022~26" — 대조 불가.
4. **미확인.** "돌파백테스트"라는 형태상 ③과 같은 하네스 계열일 가능성을 배제하지 못했고, **확인하지 않았다.**
5. 훑은 축: RS · 사전상승폭 · 패턴 · 청산. 보정 보고 없음.
6. **미실행.**

### ⑤ `winner-characteristics`
1. **없음.** 본문이 지목한 `cmp_exit.py`는 `scripts/`·`scripts/_*`에 **없다**.
2. 판별 불가.
3. "903레코드 리치데이터셋, 19일 두 상승장, 5샤드 병렬수집".
4. 미확인.
5. 훑은 축: pace 4구간 · RS 4구간 · 패턴 3종 · 누적/스파이크 2종. 보정 보고 없음.
6. **미실행.**
7. 참고 사실: **이 메모리는 26-08-23에 자기 수치 일부를 이미 철회했다**(12·16·19-21행 — "자산곡선·우세율 수치는 인용하지 말 것", 룩어헤드 2건 + 밴드폭 0 정렬).

### ⑥ `scorecard-winner-traits`
1. **없음.** `src/lib/scorecard.ts`(`ea0118b4`, 304행)는 체결장부 → 왕복 집계까지만 한다. "셋업 보유 여부 × 승률", "국면 × 승률" 분해 코드는 없다.
2. 판별 불가.
3. "정산표 53왕복(19승 34패, 승률 35.8%)".
4. 미확인. 본문은 "매수 당시 표시등급·RS를 git 스냅샷으로 복원"이라 적는다 — **복원 코드가 없어 as-of 여부를 확인하지 못했다.**
5. **보고됨.** 28행: "n=53이라 **다중비교 보정 통과 축은 0개**(최선 p=0.036~0.041)".
6. **미실행.**

### ⑦ `threshold-newcomer-caution`
1. **있음, 단 git 미추적.** `scripts/_build_passmatrix.py`(494행) — `git ls-files`가 **빈 결과**이고 `git status`에 `??`로 뜬다. 함께 지목된 `scripts/screen_earnings_calendar.py`(`b2885756`, 440행)는 추적됨.
2. 미대조.
3. "6년 전수" · 통과행렬.
4. 미확인.
5. 보정 보고 없음.
6. **미실행.** — **⚠️ 미커밋 파일은 다음 브랜치 정리에서 사라질 수 있다.** 사실만 적는다.

### ⑧ `correction-bottom-leader-factors`
1. **없음.** 본문은 "FDR 전종목 과거OHLCV(`_hist`, 5샤드병렬)"만 적고 경로가 없다. 해당 산출물을 찾지 못했다.
2. 판별 불가.
3. "코스피 조정 바닥 9개(2022~2025) · **21,026 관측** · 전방 120일 5분위".
4. 미확인. 본문은 **생존편향**을 스스로 적는다(19행: 상폐 약체 누락으로 저항력 과소측정).
5. **바닥 9개 × 요인 5종 이상 × 5분위**를 훑었다. **보정 보고 없음.** 본문은 "단일바닥은 신기루" 형태로 표본 하나의 위험만 적는다.
6. **미실행.** 🚨 `_hist` 재수집은 FDR 호출이므로 안전조항에 따라 **시도하지 않았다.**

### ⑨ `gate-relaxation-backtest-verdict`
1. **하네스만 있음.** `scripts/backtest_volatility_pilot.py`(`2f5f1538`, 424행) 존재. 같은날 부호검정 p 3개·슬롯5 짝비교 500회·117건 분해를 내는 **분석 코드는 없다.**
2. 판별 불가.
3. "⑦만 풀어 들어온 127건(결착 **117**)" · "슬롯5 매수가능일 137일".
4. 미확인.
5. **부분 보고.** 본문 22행: "정렬규칙(거래대금/RS/ATR)을 바꾸면 순위가 뒤집히므로 **어느 것도 유의하다고 말하면 안 됨**". 보정 자체는 없음.
6. **미실행.**

### ⑩ `ipo-track-validation`
1. **문서만 있음.** `docs/research/2026-08-08-ipo-track-replay-findings.md` 존재. `scripts/screen_ipo_track.py`(`4355da68`, 115행)는 **운영용 스크리너**다. 재현 검증(에피소드 106건 집계) 코드는 없다.
2. 판별 불가.
3. "2025-01~2026-02 상장 **131종목 · 에피소드 106건**".
4. 본문이 **"룩어헤드 0건"을 주장**한다. **그 주장을 확인할 코드가 없어 확인하지 못했다.**
5. 훑은 축: iRS 문턱 · 상장경과일 · 고점거리 · 손절폭. 보정 보고 없음. 본문은 "소표본"이라 적는다.
6. **미실행.**

### ⑪ `volatility-pilot-backtest`
1. **하네스만 있음.** `scripts/backtest_volatility_pilot.py`(`2f5f1538`) — 이 세션이 6개 연도 전부 동일성 확인을 마친 그 파일이다. **파일 안에 국면 축이 없다**(`regime`/`국면`/`20MA`/`ma20` 검색 결과는 무관한 `sma150/sma200` 1행뿐). +2.68 / −1.02는 `market-regime.json`을 쓰는 **별도 분해**에서 나왔고, 그 코드는 없다.
2. 판별 불가.
3. "2025-11-26~2026-08-21(180 스캔일) · **614거래 · 고유 350종목**".
4. 본문이 **"룩어헤드 필드 보정"을 스스로 적는다**(갭업 체결가 포함). 국면 분해 자체의 as-of 여부는 미확인.
5. 본문이 "저변동 우위"를 같은날 비교 p=.88로 기각하며 **국면 축만 채택**했다고 적는다. 보정 보고 없음.
6. **미실행.**

### ⑫ `liquidity-burden-display`
1. **1.02는 코드가 아니라 주석이다.** `scripts/screen_buy_recommendations.py:84`(`1ea08eb4`) — `liquidity_fields()`의 **독스트링 문장**: "실측 손절일 거래대금 중앙값 = ADV20의 1.02배". 34건을 세는 코드는 없다.
2. **하드코딩·비계산** (1.02). 단 **N/M 공식 자체는 계산된다** — 같은 함수 `:95-101`이 `turnover = c*v`, `adv20 = mean(t20)`, `burden_pct`, `split_risk_krw`를 실제로 산출한다. `POSITION_KRW = 10_000_000`은 `scripts/canslim_lib/strategy_params.py:37`.
3. "34건 실측" — 세는 코드 없음.
4. 미확인.
5. 문턱 5%/30%를 "슬리피지 실측 앵커"로 적으나 산출 코드 없음.
6. **미실행.**

---

## 감사하지 못한 것 — 이름을 적는다

1. **재현을 한 건도 하지 않았다.** 12건 전부 "그 코드가 그 숫자를 낸다"를 **실행으로** 확인하지 않았다. ③만 리포트 HTML과 문자 대조를 했다.
2. **`코드 없음` 8건은 "찾지 못했다"이지 "존재하지 않는다"가 아니다.** 과거 세션의 스크래치패드(세션별 임시 폴더)는 이미 정리되었을 수 있고, 나는 그것을 열어 보지 않았다.
3. **④의 룩어헤드 여부** — 백테스트 계열을 확인하지 않았다.
4. **⑥의 as-of 복원** — git 스냅샷 복원 코드가 없어 확인 못 했다.
5. **⑩의 "룩어헤드 0건" 주장** — 확인할 코드가 없다.
6. **`_build_passmatrix.py`(⑦)는 있으나 돌리지 않았다.** 494행.
7. **`monthly-10ma-breakout-validation`과 `volume-timing-spike`는 범위에서 빠졌다**(두뇌 세션 결정). **감사하지 않았다.**
8. **메모리 본문 대 `description`/`MEMORY.md` 색인줄의 어긋남**은 ①③에서만 확인했다. 나머지 10건은 대조하지 않았다.

---

## 17~20번 산출물은 어디서 감사되었나 — **면제가 아니라 지목**

이 파일은 17~20번을 감사하지 않았다. 그 몫은 **검증 세션**에 있고, 결과물은 다음에 있다:

| 대상 | 검증 파일 |
|---|---|
| 17 (비용·복리·슬롯) | `research/handoff/verdicts/17-fees-compounding-slots.md` |
| 18 (슬롯 선택 원인) | `verdicts/18-stage0-slot-selection.md` · `verdicts/18d-19-stage0.md` · `verdicts/18-19-preregistration-audit.md` |
| 19 (거래량 축) | `verdicts/18d-19-stage0.md` · `verdicts/18-19-preregistration-audit.md` |
| 20 (월별 점유) | `verdicts/20-preregistration-audit.md` |
| 21 (이 과제) | `verdicts/21-preregistration-audit.md` |

**내가 그 파일들의 내용을 이 감사에 반영하지는 않았다.** 위치만 지목한다.

---

## 한계 — 이 감사가 못 하는 것

**검증 세션 자신의 숫자를 아무도 감사하지 않는다.**

이 감사는 메모리 12건을 코드에 비추어 보았다. 그런데 **이 세션의 17~20번 산출물을 검사한 것은 검증 세션**이고,
**검증 세션이 그 과정에서 만든 숫자를 검사하는 세 번째 자리는 없다.** 구체적으로 다음 값들이 그렇다:

- 동일성 **0.8877**
- MDE **3.07**
- **+45.9%p** (20번 C팔의 대수적 상한)
- 슬롯 20 상한

**이 값들은 이번 감사의 대상이 아니었고, 다른 어디에서도 대상이 되지 않았다.**
그리고 **이 파일 자체도 마찬가지다** — 위 판정표의 12행은 내가 읽고 내가 적은 것이고, 대조한 사람은 없다.

이 파일은 **숫자와 사실만** 담는다. 판정 문장은 쓰지 않았다.

---

# 🚨 정정 (2026-08-23, 23번 작업 중 발견) — **제 탐색 범위가 틀렸습니다**

위 감사는 **`scripts/`·`src/`·`docs/`·`public/data/`만** 훑었습니다.
23번을 하다가 **`.cache/bt5y/`와 세션 스크래치패드에 산출 코드가 살아 있는 것**을 봤습니다.

## 무엇을 안 봤나

| 위치 | 내용 |
|---|---|
| `.cache/bt5y/*.py` | **7개** — `cmp_exit.py` · `analyze.py` · `analyze2.py` · `verify307.py` · `verify_drag.py` · `regime_long.py` · `nasdaq_test.py` |
| 세션 스크래치패드 | **6개 폴더가 살아 있고 `.py` 562개** (`aff259ca` 46 · `c9b9862c` 453 · `6169d0c4` 36 · `990b6503` 12 · `f3bf3bbc` 15) |

위 감사 본문에 *"과거 세션의 스크래치패드는 이미 정리되었을 수 있고, 나는 그것을 열어 보지 않았다"*고 적었는데,
**정리되지 않았고, 열어 보지 않은 것은 사실이며, 열어 보니 코드가 있었습니다.**

## 확인된 정정 두 건

### ⑤ `winner-characteristics` — **`cmp_exit.py`는 있습니다**
위에서 *"`cmp_exit.py`는 `scripts/`·`scripts/_*`에 **없다**"*고 적었습니다.
**있습니다** — `.cache/bt5y/cmp_exit.py` (3,154바이트 · **git 미추적**).
머리말: *"청산 규칙 비교 — 슬롯5 자산곡선으로 판정(거래당 수익률로 판정 금지: 사전등록)"*.

**단, 감사한 주장의 산출 코드는 아닙니다** — 파일 안에 `rs`·`pace`·`RS` 문자열이 **0회**입니다.
→ **출처 `코드 없음`(RS 판별자·903레코드에 대해)은 유지되나, "파일이 없다"는 서술은 틀렸습니다.**

### ② `actionable-leading-discriminators` — **`코드 없음`은 틀렸습니다**
산출 코드가 있습니다:
`…/aff259ca-1adc-48bc-b0c7-37693e4ef158/scratchpad/build_events.py`
머리말: *"Actionable+leading-sector cohort: events, entries, outcomes, discriminators."*
(같은 폴더에 `aggregate.py`·`adversarial_verify.py`·`verify_stats.py`·`finalize.py` 등 46개)

**그리고 이제 룩어헤드를 추론이 아니라 코드로 확인했습니다** (`build_events.py:124-129`):

```python
relvol = None
if entry_i >= 10:
    w = v[max(0, entry_i - 50):entry_i]
    m = mean(w) if w else None
    if m and m > 0:
        relvol = v[entry_i] / m
```

필드명 **`relvol_entry`** · **`v[entry_i]` = 진입일 자신의 종일 거래량 ÷ 직전 50일 평균**.
**`rel_volume(series, ni)`·`rel_vol_entry_LOOKAHEAD_DO_NOT_FILTER`와 구조가 같습니다.**

| ② 두 축 | 정정 전 | **정정 후** |
|---|---|---|
| 출처 | `코드 없음` | **`재현 미실행`** (코드 존재 · 저장소 밖 · git 미추적) |
| 건전성 | `룩어헤드`(본문에서 추론) | **`룩어헤드`(코드에서 확인)** |

## 아직 정정하지 못한 것

**나머지 `코드 없음` 6행(①④⑥⑧⑨⑩⑪)과 `하드코딩·비계산` 1행(⑫)은 다시 보지 않았습니다.**
다만 주제어 검색에서 **관련 파일이 존재하는 것**까지는 봤습니다:

| 주장 | 스크래치패드에서 걸린 파일(일부) |
|---|---|
| ⑥ 정산표 | `c9b9862c :: cf.py · cf2.py · cf3.py · classify.py` · `aff259ca :: verify_stats.py` |
| ⑦ 통과행렬 | `aff259ca :: build_passmatrix.py · av_load.py · av_matrix_check.py · finalize_newcomer.py` |
| ⑧ 바닥 | `c9b9862c :: pattern_test.py · price_test.py · regime_test.py · turnover_sameday.py` |
| ⑫ 유동성 | `c9b9862c :: _liq.py` |
| ⑩ IPO | `990b6503 :: repro_edges.py` |

**이것들이 그 숫자를 내는 코드인지는 확인하지 않았습니다.** 파일이 걸렸다는 사실까지입니다.

## 이 정정이 위 판정표에 미치는 범위

- **`코드 없음` 8행은 "저장소 안에 없다"까지만 유효합니다.** "존재하지 않는다"로 읽으면 안 됩니다.
- **② 한 행은 명백히 틀렸고 위에서 고쳤습니다.**
- **⑤의 파일 부재 서술이 틀렸고 위에서 고쳤습니다**(주장에 대한 출처 판정은 유지).
- **21번 전체를 다시 돌리려면 스크래치패드 562개를 범위에 넣어야 합니다.** 이번에는 하지 않았습니다.

> ⚠️ **스크래치패드는 세션별 임시 폴더이고 `.cache`는 한 번 사고로 지워진 적이 있습니다**
> (메모리 `cache-loss-and-backup`). **여기 있는 산출 코드는 백업 대상이 아닙니다.**
