========================================================================================
_runv — **검사하고 «나서» 돌린다** · 대상 `179-sector-history.py`
========================================================================================
  ✅ ① 서식(`_fmtcheck`)
  ✅ ② 컴파일
  ✅ ③ 정의 전 사용(모듈 수준)

  ▶ 검사 통과 — 돌린다
========================================================================================
========================================================================================================
179 - **「업종 «변경»이 «얼마나» 잦은가」** (남은 물음 ⑤ · «자료 읽기»)
========================================================================================================

> 조사 세션 · 2026-09-04 · `scripts/179-sector-history.py` · **문서는 이 출력 그 자체**(유형 48)

## 🚨 **묻는 것 — 「«이력»이 «있나»」가 «먼저**

```
`174`: 「«섹터 라벨» «자체»가 «스냅숏»」 ⇒ **«작은» 룩어헤드**
   그때 나는 **「없어 «보인다»」**고 적었는데 — 그건 **«안 찾아본»** 것이었다(유형 35)
⇒ ✅ 이 문서는 **«찾아본»** 것이다. **«명령»과 «돌린 자리»를 «같이» 적는다**
```

## 1. ㉠ **`tickers.csv` — «열 목록»에 「업종 «이력»」이 «있나**

```
🔎 명령 `python -c "zipfile.ZipFile('tickers.csv.zip')…"` · 돌린 자리 `D:\stock-data\sharadar`

**열 28개 «전부»:**
   `table` · `permaticker` · `ticker` · `name`
   `exchange` · `isdelisted` · `category` · `cusips`
   `siccode` · `sicsector` · `sicindustry` · `figi`
   `famaindustry` · `sector` · `industry` · `scalemarketcap`
   `scalerevenue` · `relatedtickers` · `currency` · `location`
   `lastupdated` · `firstadded` · `firstpricedate` · `lastpricedate`
   `firstquarter` · `lastquarter` · `secfilings` · `companysite`

**업종을 담은 열 — `siccode` · `sicsector` · `sicindustry` · `famaindustry` · `sector` · `industry`**
**날짜를 담은 열 — `lastupdated` · `firstadded` · `firstpricedate` · `lastpricedate` · `firstquarter` · `lastquarter`**

## ⇒ 🔴 **업종 열 여섯 «전부» «값이 «하나»»다 — 「언제부터 그랬나」를 담은 열이 «없다**
   (`lastupdated` 는 「«이 줄»이 마지막으로 손댄 날」이지 「업종이 «바뀐» 날」이 «아니다»)

**㉡ 「한 줄/종목」이 «맞나»** — SEP 행 **20,941** · 서로 다른 티커 **20,941** · **중복 0개**
   ⇒ ✅ **한 티커에 한 줄**이다 ⇒ **이 파일 «안»에는 «이력»이 «있을 수가» 없다**
```

## 2. 🔴🔴 **㉢ — 「없다」로 «닫으려다» 「«다른 파일»에 «있다»」를 찾았다**

```
🔎 명령 `actions.csv.zip` 의 `action` 열 «전부» 세기 · 돌린 자리 `D:\stock-data\sharadar`

**`action` 종류 25개 중 «업종»에 해당하는 것:**
   `sicchangeto`   — **2,725** 건
   `sicchangefrom` — **2,725** 건

## ⇒ 🔴 **「이력이 «없다»」는 «틀렸다** — **«날짜» 붙은 업종 변경이 «있다»**

🚨 **그런데 «자»가 «다르다»**(또 유형 67):
   우리 판이 쓰는 라벨 — `61a-build-monthly.py:32` **`r.get("sector")`** (Sharadar 자체 분류)
   `actions.csv` 가 주는 이력 — **`siccode`** (미국 SIC 코드)
   ⇒ ⛔ **「우리가 «쓰는» 자의 이력」은 «여전히» «없다**
   ⇒ ✅ **다만 SIC 를 «대리»로 쓰면 「얼마나 잦은가」의 «크기»는 «잴 수» 있다**
```

## 3. ✅ **크기 — 「«대리»로」 잰다**

```
**«대분류»(SIC division 10개)가 «바뀐» 것만 «업종 변경»으로 센다**
   (`6770 금융 → 4911 운수·공익` 같은 것. 같은 대분류 안의 코드 변경은 «업종»이 아니다)

   sicchange «짝» — **2,725**  ·  그중 «대분류»가 바뀐 것 — **1,677** (**61.5%**)
   «대분류»가 바뀐 «종목» — **1,530** 개
```

## 4. ★★ **「«작은» 룩어헤드」의 «크기» — «수»가 됐다**

```
🔎 자리 `C:\Users\hanul\playground\my-stock\.cache\bt5y\sub` (우리 판이 «읽는» 그 파일)

   원시 후보 경로 — **145,287** · 서로 다른 종목 — **8,032**
   그중 «대분류» 변경이 «있었던» 종목 — **749** (**9.3%**)

## ⇒ ★ **스캔일 «뒤»에 대분류가 바뀐 경로 — 3,942 / 145,287 = «2.71%»**
   **그 경로들만 «스냅숏» 라벨이 «그때»와 «다를» 수 있다**
```

```
## ✅ **서는 문장**

① **「우리가 «쓰는» 라벨(`sector`)의 «이력»은 Sharadar 에 «없다»」** — «찾아봤고» «없다»
   («명령»·«자리»·«열 목록 28개» 를 위에 «전부» 적었다)

② 🔴 **「Sharadar 에 업종 이력이 «없다»」는 «틀린» 문장이다** — `actions.csv` 에
   **`sicchangeto/from` 이 «날짜»와 함께 2,725 건씩** 있다. **«다른 자»의 이력이다**

③ ★ **그 «대리»로 재니 「«작은» 룩어헤드」는 «경로의 2.71%»** 다
   ⇒ **「«못» 잰다」가 «아니라» 「«대리»로 «쟀고» «작다»」**로 «바뀌었다**

⛔ **못 쓸 말**
   ⛔ 「업종 이력이 «없다»」 — **«어느» 자의 이력인지 «붙이지» 않으면 «틀린» 문장**
   ⛔ 「룩어헤드가 2.71% 다」 — **SIC 는 «대리»**다. `sector` 축의 «진짜» 크기는 «여전히» 미측정
   ⛔ 「2.71% 니 «무시»해도 된다」 — **«크기»를 쟀을 뿐 «영향»은 «안» 쟀다**
   🚨 그리고 이 수는 **«원시» 후보 경로 145,287 기준**이다 — 판의 후보(16,150)와 «다른 자»다
```
