# 04 — 초수익 잠재력 점수 (페이지 주장 6)

## 페이지가 하는 말 (효과 없다는 주장)

"614건으로 확인하니 점수와 +20% 도달은 아무 관계가 없었다. 오히려 낮은 점수가 조금 나아
보이기까지 했는데, 같은 날끼리 비교하면 그 차이도 사라진다."

| 점수 | 1점 | 2점 | 3점 | 4점 | 5점 |
|---|---|---|---|---|---|
| +20% 도달 | 46.3% | 41.4% | 38.1% | 35.7% | 34.6% |

문턱은 **M14-1 표**를 쓴다(옛 "0~2렌즈 통과 = 유지"는 **폐기됨**).

## 문제 — 점수가 이벤트에 없다

`bt_*.json`에는 점수 필드가 없다. **scan_date 시점으로 다시 계산해야 한다.**

## 입력·도구

- `scripts/canslim_lib/superperf.py`
  - `compute_factors(dates, closes, highs, index_closes, look=252, window=120)`
    → `{prior_adv, rs_nh_days, rs_leads, dist_52wh}`
  - `score(rs, prior_adv, rs_nh_days, rs_leads)` → `(점수, 사유목록)`
- `rs`는 **이벤트의 `rs` 필드를 그대로 쓴다**(이미 as-of 값이다. 다시 계산하지 말 것).
- `index_closes`는 **pdata 시총가중 지수**를 쓴다(`market_cap_eok` 가중 일별 등락률 누적).
  이 지수가 코스피와 일치한다는 것은 이전 세션에서 확인됐다. FDR을 새로 받지 않는다.
- 종목 시계열은 01번 경로 자료가 아니라 **scan_date까지 잘린 과거 시계열**이 필요하다.
  01번에서 만든 수정주가 시계열을 재사용하되 `scan_date` 이하로 잘라 쓴다.

## 계산 단계

1. 이벤트 3,681건 각각에 대해 scan_date 시점 점수(0~6)를 산출.
   계산 불가(시계열 부족 등)면 `None`으로 두고 **건수를 따로 적는다.**
2. **검증용 대조 — 먼저 할 것:** 9개월 구간(2025-11-26~)만 잘라
   페이지의 점수 분포(46.3/41.4/38.1/35.7/34.6%)가 재현되는지 확인한다.
   어긋나면 **어긋난 건수와 방향을 맨 위에 적고**, 재산출 방식의 차이를 규명한 뒤 진행한다.
3. 점수별 표: 건수 · 승률 · 거래당 순수익 · 다섯 구간별 값.
4. **1순위 가설:** 점수 4점 이상 집단 vs 2점 이하 집단의 거래당 순수익 차이.
   같은날 비교(같은 entry_date 안에 두 집단이 다 있는 날만) 부호검정이 L1.
5. 슬롯5: (가) 전부 (나) 점수 3점 이상만 (다) 점수 2점 이하만 — 400회 짝비교.
   + **같은 건수 무작위 표집 대조** 필수.

## 출력 형식

```
| 점수 | 건수 | 승률 | 거래당순 | 2021 | 2022 | 2023 | 2024 | 25~26 | 9개월 재현값 | 페이지 값 |
```
+ 5렌즈 표 + 슬롯5 표

## 주의

- 이 점수는 원래 **"6개월 안에 두 배"**를 맞히려고 만든 것이다. 여기서 검정하는 건
  **"+20% 도달"** 하나뿐이다. 결과 파일에 이 범위를 명시한다.
- 점수 요인 중 `prior_adv`(직전 상승폭)는 120일 최저 대비다. 창 길이를 바꾸지 않는다.

---

# ★ v2 개정 반영

- **M9-12 범위 제한 (중요):** 표본 전원이 **RS ≥ 80**이다(두뇌 세션 실측 최소값 80).
  그런데 `superperf.score()`의 6점 중 **2점이 RS**라 표본에 그 성분의 0점이 없다.
  범위를 잘라 놓고 "효과가 없다"고 하면 뒤집힌 문턱에서 그대로 "유지"가 된다.
  → **RS 성분을 뺀 4점 만점 점수도 함께** 내고, 결과 첫 줄에
  **"이 판정은 RS 80 이상 후보 안에서의 이야기"** 라고 못 박는다.
- **M7 MDE 필수**(뒤집힌 문턱). MDE > 거래당 1.0%p면 판정불가.
- **M9-11** 9개월 대조는 참고(우리 자료 581건 vs 페이지 614건, 정본 불명). 재현 실패를 근거로 쓰지 않는다.
- **M3·M4** 슬롯5는 새 정본, 평균 체결 건수 병기.

---

# ★★ M10 적용 [2026-08-23] — 이 과제의 통계 문턱이 바뀌었다

`00a-amendments-v2.md`의 **M10**을 반드시 읽고 시작할 것. 요지:
- **seed 짝비교 p를 검정으로 쓰지 않는다.** 우세율은 "후보 선택 순서에 대한 강건성" 참고값일 뿐이다.
- 불확실성은 **연속 20~40거래일 블록 부트스트랩 1,000회**로 낸다(복제 내 seed 고정 + 부가로 seed 5개 평균 판).
- 변형이 여럿이면 BH가 아니라 **최대통계(Westfall–Young)**, **귀무도 관측과 같은 횟수로 seed 평균.**
- **"효과 있음" 주장:** L5 자리에 **"차이의 95% 구간이 0을 제외"**.
- **"효과 없음" 주장:** 유지 = 구간이 0 포함 **그리고 MDE ≤ 거래당 1.0%p** /
  폐기 = 구간이 0 제외 / **판정불가 = 구간 0 포함이나 MDE > 1.0%p**.
- **"최악 연도 제거" 열은 이제 표준**이다. 모든 표에 넣는다.

# ★ M11 적용 [2026-08-23]

**같은날 점수 4+ vs 2− 비교**라 L2 무효 → **L1의 강화판으로 흡수**(M13-1, 렌즈로 따로 세지 않음).

`00a-amendments-v2.md`의 **M11** 참조. 대체 사유를 결과 첫 줄에 적을 것.

# ★★ M12 적용 [2026-08-23] — M10보다 이쪽이 우선

**M12-1: "효과 없음"이라 최대통계 걸지 않음** — 보정 없는 개별 구간이 주 판정.
**M12-3: 최선 결과는 "확인 불가(검정력 부족)"**. MDE를 숫자로 싣고 "확인됨"이라 쓰지 않는다.
**M12-2: 구간과 MDE를 같은 통계·같은 단위로**(거래당 %p 권장), `MDE = 2.80 × 부트스트랩 차이 SD`.

`00a-amendments-v2.md`의 **M12** 전문을 읽을 것.

# ★★★ M13 적용 [2026-08-23] — M11·M12보다 이쪽이 우선

- **날 안 라벨 무작위화는 L1의 강화판**이다. 렌즈로 따로 세지 않는다.
- **L2 자리에 새 표준 렌즈: `한 해씩 빼도 1순위 통계의 부호가 유지되는가`(leave-one-year).**
  **여섯 해 전부 부호 유지 = 통과.** 이 렌즈는 **모든 과제**에 건다.
- **폐기 판정은 1순위 변형으로만.** 부가 변형이 0을 제외하면 "보정 없는 부가 소견"으로만 기록.
- 렌즈를 쓰기 전에 **"이 검정은 어떤 경우에 실패하는가?"** 를 먼저 답할 것.
  답이 "실패할 수 없다"면 렌즈가 아니라 장식이니 즉시 보고.

# ★★★★ M14 적용 [2026-08-23] — 개정 번호가 클수록 우선

- **판정은 M14-1 표**로 한다. 핵심: **CI가 0을 포함해도 "효과 없음"이 아니다.**
  구간 전체가 **거래당 ±0.5%p 안**에 들어야 동등성이고, 밖까지 뻗으면 **판정불가/확인 불가(검정력 부족)**다.
  **점추정 방향이 주장과 반대면 검정력과 무관하게 폐기.**
- **MDE 필수 보고**: `MDE = 2.80 × 부트스트랩 차이 SD`. 1순위와 **같은 단위**로.
- **leave-one-year**: 연도 = **`entry_date` 기준**, 2026(8개월·518건) 포함 + **"2026 제외 5년" 판 병기**.
- 과제별 1순위·문턱 변경은 **M14-2 표**를 볼 것.

# ★★★★★ M15 — 렌즈 정본 (이 블록이 최우선)

**렌즈는 넷이다: L1(같은날) · L2′(leave-one-year) · L3(구간 5/5) · L4(집중도).**
**원형이동 순열은 폐기.** 판정의 축은 렌즈가 아니라 **1순위 통계의 95% 구간(M14-1 표)**이다.
**유지 = M14-1 "유지" 줄 + 렌즈 4/4 / 판정불가 = 렌즈 3/4 / 폐기 = 렌즈 0~2/4.**

렌즈를 쓰기 전에 **양쪽을 다 답할 것: ①어떤 경우에 실패하는가 ②어떤 경우에 성공하는가.**
둘 중 하나라도 "없다"면 렌즈가 아니라 장식이니 즉시 보고.

# ★ M16 [2026-08-23] — 최신 개정 (이 블록이 최우선)

- **동등성 폭은 과제 단위로**(M16-2 표): 03b·04·05·10 = 거래당 ±0.5%p / **09 = 승률 ±1.5%p** /
  **08 = 동등성 미적용, M9-1 자체 규칙** / **11 = ρ ±0.02(관례값임을 명시)**.
- **"반대 부호면 무조건 폐기"는 `L2′ 여섯 해 전부에서 반대 부호가 유지될 때만`** 적용(M16-1).
- **08은 3렌즈(L2′·L3·L4) 기준 3/3·2·0~1**, **11은 렌즈 없이 자체 문턱(사후예측 p)만**(M16-3).
- 결과를 쓸 때 **"검증 실패"가 아니라 "이 자료로는 답할 수 없는 질문이었다 + 못 잡은 크기(MDE)"** 로(M16-4).

# ★ M17-5 [2026-08-23] — 지수 일치를 **숫자로** 재확인할 것

04는 pdata 시총가중 지수를 초수익 점수의 입력으로 쓴다. **틀리면 04 전체가 흔들린다.**
지금은 "이전 세션에서 코스피와 일치 확인됨"으로 **인용만** 돼 있다.
이번 검증에서 "이전에 확인됨"으로 인용된 것 중 **재현 불가**였던 사례가 있었다(9개월 577건).

→ **재현 명령에 일치 확인을 포함하고 결과 파일에 숫자로 남길 것**
  (구간 상관계수 · 구간 최종 수익률 대조). 어긋나면 거기서 멈추고 보고.

# ★ M18-1 — 표준 보고 항목 추가

**모든 팔에 대해 `슬롯5 자산곡선의 5~95% 밴드 폭`을 싣는다.**
**폭이 0(또는 0에 가까우면) 그 축의 우세율·짝비교 값을 인용하지 않는다** —
그 팔은 분포가 아니라 **숫자 하나**이고, 우세율은 두 분포의 비교가 아니라 **고정값의 백분위**다.
(실측: 거래대금은 858일 중 동점 0일이라 정렬 팔의 밴드 폭이 정확히 0이었다.)

# ★★ 04 표본 사전 실측 + 해석 규칙 [2026-08-23, 결과 전]

| 항목 | 값 |
|---|---|
| RS 결측 | **0건** (3,681/3,681) |
| RS 범위 | **80 ~ 99** |
| RS 80~89 (점수 1점) | **1,647건 (44.7%)** |
| RS 90+ (점수 2점) | **2,034건 (55.3%)** |

**"2점 이하" 집단은 구조적으로 비지 않는다** — 44.7%가 RS 1점이라 나머지 요인이 0~1이면 총 1~2점이 된다.
(그래도 **두 집단 건수 + L1 성립 날 수를 결과 맨 위에** 싣는 요구는 그대로.)

## ★ 두 판이 갈릴 때의 해석을 **미리 확정한다** (결과 보고 고르지 못하게)

6점 중 **2점이 RS**이고 그 RS가 **55/45로 크게 갈린다.**
즉 "4점 이상 vs 2점 이하"는 상당 부분 **"RS 90 이상인가"의 다른 이름**이다.
그런데 페이지의 주장은 RS가 아니라 **합성 점수**에 대한 것이다.

| 6점 만점판 | RS 제거 4점판 | **해석(사전 확정)** |
|---|---|---|
| 효과 있음 | 효과 없음 | **"RS가 끌고 나머지 요인은 무효"** — 페이지 주장은 *합성 점수에 대해서는* 유지 |
| 효과 있음 | 효과 있음 | **페이지 주장 폐기** |
| 효과 없음 | 효과 없음 | **확인 불가(검정력 부족)** |

## ⚠️ RS 신호가 보여도 기존 확정치와 연결 금지

메모리 `winner-characteristics` 의 **"슬롯5를 RS순으로 바꾸면 52%/+5.5%"** 를 근거로 쓰지 말 것.
그 값은 903레코드 시절이고 **이번에 고친 것들(같은날 재진입·seed 짝비교·유니버스·ambiguous) 이전**이며,
**RS는 동점이 거의 없어 밴드 폭이 0인 결정적 정렬**이라 그 "우세율"은 고정값의 백분위였다.
→ 04에서 RS 신호가 보이면 그것은 **새로 확인해야 할 가설**이지 기존 확정치의 재확인이 아니다.
(해당 메모리에 2026-08-23 주의 블록을 넣어 두었다.)

# ★ M19 — 판정축 우선 · 평균/중앙값 병기

- **판정축(1순위 통계의 구간)이 판정을 정하고, 렌즈는 강등만 시킨다.**
  렌즈가 유의해도 **판정축이 못 세운 효과를 만들어 낼 수 없다**(그러면 "여러 통계 중 유의한 것 고르기"가 된다).
- **판정축은 평균**으로 고정(계좌 수익은 거래별로 더해진다). **평균·중앙값 구간을 나란히 싣고
  부호·유의성이 갈리면 명시**한다. 중앙값만 유의하면 **꼬리가 두껍다는 신호**이므로
  그 통계에도 **L4(집중도)**를 걸어 상위 5건 제거값을 함께 낸다.
- **분기·필터 인자는 로그와 결과 표에 값을 찍는다.** 두 판의 값이 완전히 같으면 **필터 적용부터 의심**한다.

# ★★ M17-5 결론 — 지수 정본 확정 [2026-08-23, 결과 전]

## 무엇이 어긋났나 (조사 세션 실측)

`regime_long.json`의 `cw`(pdata 시총가중)와 `kospi`(FDR KS11), 2021-02-01~2026-08-20 · 1,360거래일:
- **일별 등락률 상관 0.9965**, SD도 거의 같음(1.7344 vs 1.7560)
- **그런데 구간 총수익이 +102.2% vs +124.2%로 22.0%p 벌어짐**

**원인은 계산 오류가 아니라 구성 차이다** — `cw`는 **KOSPI + KOSDAQ 전체** 시총가중이고
코스피 지수는 **KOSPI만**이다. **서로 다른 지수다.**

## 정본 결정 — **운영 코드와 같게 간다**

`scripts/screen_buy_recommendations.py`를 확인했다(이 점수를 사용자에게 실제로 보여 주는 코드):

```python
# 시장지수 KS11(코스피)/KQ11(코스닥) → {market: {date: close}}
for market, code in (("KOSPI", "KS11"), ("KOSDAQ", "KQ11")):
```

**운영은 하나의 지수가 아니라 `종목의 시장에 맞는 지수`를 쓴다** — KOSPI 종목은 KS11, KOSDAQ 종목은 KQ11.

**→ 정본: 시장별 지수(KOSPI→KS11 · KOSDAQ→KQ11), FDR로 2020년부터 취득.**
페이지의 주장은 **사용자가 실제로 본 점수**에 대한 것이므로, 재현도 그 정의로 해야 한다.
(운영 코드는 `"2024-01-01"`부터 받지만 우리는 백테스트 구간을 덮게 더 이른 날짜부터 받는다.)

**부가 대조 두 판(조사 세션 제안 채택):**
1. **`cw`(pdata 시총가중) 판** — 지수 선택이 점수를 얼마나 흔드는지
2. **점수가 달라지는 이벤트 건수**를 세어 영향 크기를 **숫자로** 낸다
   (superperf가 지수를 쓰는 곳은 `RS선 = 종가/지수`의 252일 신고가까지 며칠 · RS선 선행 여부뿐이라
    느린 누적 괴리보다 단기 상관이 중요하다. 그래도 경계 사례가 흔들릴 수 있으니 **실제 흔들린 건수로** 답한다.)

**FDR 취득이 실패하면** cw를 정본으로 쓰되 **그 사실과 위 22.0%p 괴리를 결과 첫 줄에 적는다.**

# ★★★ M21 [2026-08-23] — 정의를 고르는 손잡이를 잠급니다 (실행 중 전달)

1. **정의는 운영 코드가 정한다**(KS11/KQ11). 근거는 **재현 결과가 아니라**
   "사용자가 본 점수가 그것"이라는 사실이다.
2. **9개월 대조(46.3/41.4/38.1/35.7/34.6%)는 검증이지 선택이 아니다.**
   재현되면 정의 선택이 확인된 것이고, **재현 안 되면 그것이 보고할 발견**이다.
   **정의를 바꾸는 사유가 아니다.** ← 이걸 어기면 04는 무효다.
3. **cw 판은 민감도로만.** 어느 쪽이 페이지와 더 가까운지로 1순위를 정하지 않는다.

**점검 대상 교체(M21-2):** cw-코스피 일치가 아니라 **KS11/KQ11 적재·정렬 무결성**을 숫자로 —
구간 커버리지(첫날·마지막날·거래일 수) · **결측일 수**(종목 시계열엔 있는데 지수에 없는 날) ·
**하루 밀림 여부**. 지수가 하루만 밀려도 `rs_nh_days`·`rs_leads`가 통째로 흔들리고 그건 6점 중 2점이다.

**검산(M21-3):** `rs`는 이벤트 필드 그대로, `prior_adv`는 지수를 안 쓴다.
→ **두 판의 점수 차이는 `rs_nh_days`·`rs_leads`에서만 나와야 한다.
   다른 요인에서 차이가 나오면 구현 오류다.**
