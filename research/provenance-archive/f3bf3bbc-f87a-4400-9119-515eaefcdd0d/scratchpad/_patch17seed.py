# -*- coding: utf-8 -*-
import pathlib

p = pathlib.Path("research/handoff/results/17-fee-and-compounding.md")
t = p.read_text(encoding="utf-8")

old = """**①→② 슬롯 제약의 선택 효과 (직접 측정, 20 seed):**
전체 3,776건 거래당 **+0.1950%p** vs **슬롯5가 실제로 산 428건 거래당 +0.0158%p**
→ **−0.1792%p.** **슬롯이 고르는 거래가 전체 평균보다 훨씬 나쁘다.**"""
new = """**①→② 슬롯 제약의 선택 효과:**

> ### ⚠️ **이 값은 18번에서 200 seed로 다시 쟀고, 크기가 줄었으며, 확인 불가로 나왔다**
>
> | | 값 | 비고 |
> |---|---:|---|
> | **처음 적은 값** | **−0.1792%p** | **20 seed였음** (전체 +0.1950 vs 체결분 +0.0158) |
> | **18번 재측정(200 seed)** | **중앙 −0.0905%p** · 평균 −0.1322%p | **정본** |
>
> **그리고 18번 0단계는 이 격차가 이 자료로 확인 불가라고 답했다** —
> seed 분위 **−1.1724 ~ +0.9788**, 날짜 블록 부트스트랩 **−1.8134 ~ +1.2967**로 **둘 다 0을 포함**하고
> **MDE가 1.52 ~ 2.19%p로 관측 격차의 17~24배**다.
> **아래 −16.6%p는 지우지 않고 남기되, "확인 불가"라는 단서를 달고 읽어야 한다.**
> **판정은 두뇌·검증 세션의 몫이다.**"""
assert t.count(old) == 1
t = t.replace(old, new)

old2 = """| ② **슬롯5가 실제로 사는 428건 기준**으로 다시 | **+1.4%** | **−16.6%p** ← **슬롯 제약의 선택 효과** |"""
new2 = """| ② **슬롯5가 실제로 사는 428건 기준**으로 다시 | **+1.4%** | **−16.6%p** ← **슬롯 제약의 선택 효과**<br>**(20 seed 기준. 18번 200 seed 재측정에서 격차가 절반으로 줄고 "확인 불가"로 나왔다)** |"""
assert t.count(old2) == 1
t = t.replace(old2, new2)

old3 = """### ⚠️ 원인 가설 하나 — **아직 세우지 않는다**"""
new3 = """### ⚠️ **①→② 항 전체가 18번에서 확인 불가로 나왔다**

18번 0단계(200 seed · 사전등록 관문)의 결론은 **"슬롯 선택 열화는 이 자료로 확인 불가"**다.
**아래 "붐비는 날" 가설은 그 위에 얹힌 것이므로 더더욱 세울 수 없다.**

### ⚠️ 원인 가설 하나 — **아직 세우지 않는다**"""
assert t.count(old3) == 1
t = t.replace(old3, new3)

old4 = """| 슬롯 | 체결분 거래당 | ① 전체평균 기준 | ② 체결분 기준 | (선택 효과) | ③ 실제 곱 | **(분산 손실)** | ④ 실측 |"""
new4 = """**⚠️ 아래 "체결분 거래당"과 "선택 효과" 열은 10 seed 기준이다.**
**17e에서 200 seed로 재니 그 값의 seed 5~95% 폭이 0.66 ~ 2.39%p로, 열 자체가 잡음이다**(아래 절).

| 슬롯 | 체결분 거래당 | ① 전체평균 기준 | ② 체결분 기준 | (선택 효과) | ③ 실제 곱 | **(분산 손실)** | ④ 실측 |"""
assert t.count(old4) == 1
t = t.replace(old4, new4)
p.write_text(t, encoding="utf-8")
print("17 ok")
