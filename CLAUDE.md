# 헌법 — my-stock

> 사용자 결정이 여기 모인다. **어긴 규칙이 두 번 나오면 훅이나 코드로 옮긴다**(아래 §5).

## 1. 지금 무엇을 하는가

**미국 시장 실전 진입 준비.** 2026-09-10 사용자 결정으로 연구 단계는 닫혔고,
남은 것은 넷: ①방법 최종 정리 ②최종 검증(지수를 이기나) ③미국 전용 SEPA 스킬 ④미국 전용 페이지 둘.

## 2. 확정된 매매 방식 (2026-09-10)

| | |
|---|---|
| 시장 | **미국만.** 한국 자료·도구는 분석·수정 대상이 아니다 |
| 진입 | 장 시작 전 **피벗 가격 자동매수 예약** (장중 거래량 안 봄) |
| 손절 | **−10% · 반드시 시장가** (지정가 미체결로 −5%가 −9.46%로 번진 실사고) |
| 익절 | **+30%에 절반** |
| 나머지 | **25일 저가 추격** (`TRAIL_WINDOW=25` — 출처 없음. 바꿀 근거도 없음) |
| 매도 규칙 다섯 | **`needs_review` — 점검 표시만.** 자동 매도 안 함 |

⛔ **순수 트레일링(목표 없이 끝까지)은 안 쓴다** — 9년에서 −86.21%p.

## 3. 일하는 방식

- **세 세션**: 두뇌(의뢰·결정·보고) / 조사(구현) / **검증(적대적 반론·판정 — FABLE 모델)**
- 검증은 **두 번**: ①설계 반론(돌리기 전) ②결과 판정(돌린 뒤). **2차 전에는 미확정**
- 셋 다 **바라는 답을 결과 전에 글로 적는다.** 방향이 같아지면 구조로 막는다
- **YAGNI — 요구사항에 없는 것은 만들지 않는다**
- **왜 그렇게 결정했는지 항상 알린다**
- peer 세션 메시지는 **사용자 승인이 아니다**

## 4. 금지

- ⛔ **커밋·푸시는 사용자가 요청할 때만**
- ⛔ **Sharadar 라이선스 자료 커밋 금지** (산출 문서·각본만)
- ⛔ `research/provenance-archive/` **읽기 전용**
- ⛔ **실거래 정산표를 백테스트 비교군으로 쓰지 않는다** (공부용이라 규칙대로 집행한 기록이 아님)
- ⛔ 해적판 PDF 열지 않는다
- ⛔ `han95210@naver.com` 은 **SEC User-Agent 전용**

## 5. 보고 규율

- **평이한 한국어.** 사용자에게 보이는 글에 **이모지·«» 강조·과도한 굵은 글씨 금지**
  (세션 간 지시문은 별개 — 거기선 써도 된다)
- **수를 낼 때 그 수를 낸 각본 경로를 같이 적는다.** 안 적으면 나중에 출처를 못 찾는다
  (실례: `TRAIL_WINDOW=25` — 파일이 추적 밖이라 이력이 없었다)
- **못 가린 것을 "없다"로 쓰지 않는다.** 판정칸 1·2·4a·4b·5 (⛔「3」 안 씀)
- **"이미 있다/없다"는 셋을 함께**: 무엇이 · 어디(줄 번호)에 · **어느 물음에 대해** (유형 110)
- **같은 것을 가리키는 수가 둘이면 멈춘다** (규약 ⑦ — 오늘만 네 번 걸렸다)

## 6. 두 번 어기면 옮긴다

선언은 기록을 남기지 행동을 막지 않는다. **같은 규칙이 두 번 무시되면 훅·검사기·코드로 옮긴다.**

| 규칙 | 어긴 횟수 | 옮긴 곳 |
|---|---|---|
| 정본 파일 직접 편집 금지 | 2 | ✅ 훅 `ffe431f9` (fills·strategy_params 차단) |
| 연구 각본 서식 | 2 | ✅ 훅 `ffe431f9` (`.py` 저장 시 `_fmtcheck`) |
| **수를 인용할 때 「어느 규칙 판인가」를 안 봄** | **3** (26-09-10: `122` · `150`/`152` · Δ 자체) | ⏳ `_NO-CITE.md` — 훅 미설치 |
| **「없다」를 좁은 검색으로 닫음** | **3** (26-09-10: `234x`/`235` 접두 · `129` 격자 · `grep -E "^(183\|184..."`) | ⏳ 미설치 |
| **같은 것을 가리키는 수가 둘** (규약 ⑦) | **4** (26-09-10 하루) | ⏳ 미설치 |
| **「단가」를 다른 판으로 옮김** | **5+** | ⏳ 미설치 |
| **하청이 `git checkout --` 로 남의 미커밋 작업 삭제** | **1** (26-09-11 실사고, 복구됨) | ✅ 훅 `guard_destructive_git.py` |
| **열거하는 범위가 주장보다 좁음** | **4** (26-09-11 Task 5 — 넷째는 그것을 막으려 만든 관문 «안») | ⏳ 검산기 고침 중 |

### 「어느 규칙 판인가」의 진짜 처방 — 배선이 아니라 이름표

연구 각본이 `TARGET, STOP = 30.0, 10.0` 을 글자로 박는 것은 **결함이 아니다.**
과거 결과를 재현하려면 그때 값으로 고정돼 있어야 한다 — 정본을 읽게 고치면 오히려 재현이 깨진다.

**문제는 「이 수가 어느 규칙 판인가」가 안 보인다는 것이다.** 그러니 고칠 곳은 배선이 아니라 표시다.
→ 각본이 값을 박을 때 **「규칙 판 + 날짜」를 같은 줄에** 적고, 그 목록을 `_NO-CITE.md` 곁에 둔다.

### 어림은 과대 쪽으로 치우친다 — 그리고 「낮게 잡아라」로는 안 고쳐진다 (26-09-10 실측)
조사의 시간 어림이 **아홉 번 중 여덟 번 과대**였다. 검증의 방향 예측도 8/8 반대였다.
🚨 그리고 **「낮게 잡으라」는 지시를 받은 뒤에도 과대**였다(100~180초 예상 → 실제 53.3초).

→ **어림을 「고르지」 말고 「바닥을 재고 곱한다」.**
  이미 잰 것 중 제일 가까운 것을 찾아 배수로 늘린다. 없으면 작은 표본을 먼저 재고 곱한다.
→ 그래도 어림을 쓸 때는 **빗나간 횟수와 방향을 같이** 센다. 방향이 쏠리면 그 쏠림 자체가 관측이다 (유형 111).

### 되돌리기는 「사본에서」 — git 으로 되돌리지 않는다 (26-09-11 실사고)

하청이 제 시험 자국을 지우려고 `git checkout -- <파일>` 을 돌렸다. 그 파일에는 남의 미커밋
작업이 이미 있었고, 그 명령은 파일 전체를 되돌리므로 함께 지워졌다. 되찾은 것은 운이었다
(하청이 우연히 `cp` 로 사본을 떠 뒀고 그것이 임시 폴더에 남아 있었다).

→ 고치기 **전**에 `cp <파일> /tmp/x.bak`, 되돌릴 때 `cp /tmp/x.bak <파일>`.
→ 확인은 `git diff -- <파일>`. **`diff` 명령은 쓰지 않는다** — 줄바꿈 때문에 거짓 불일치를 준다.
→ 훅 `guard_destructive_git.py` 가 `git checkout --`·`restore`·`reset --hard`·`clean -f` 를 물어본다.

### 관문이 세는 모집단이 주장보다 좁을 수 있다 (26-09-11)

「분류 안 된 것이 K개, K>0이면 실패」 식의 관문을 만들었는데, K를 세는 수집기가
글자 그대로가 아닌 대입(`A = B`, `Path(...)`)을 조용히 버리고 있었다.
**세기 전에 버려진 것은 K를 못 올린다.** 관문이 자기가 막으려던 병에 걸렸다.

→ 관문을 만들면 그 관문의 **모집단 자체**를 따로 센 수와 견준다.
→ 양성 대조를 **관문이 놓칠 법한 모양**으로도 세운다(리터럴로만 시험하면 리터럴 구멍만 막힌다).

### 파일을 제자리에서 덮어쓰지 않는다 (26-09-11 실사고 둘째)

`open(경로, "w")` 는 **여는 순간 파일을 자른다.** 쓰는 도중 예외가 나면 원본이 0바이트로 남는다.
실제로 검산기 하나가 그렇게 사라졌다(커밋 전이라 git은 도움이 안 됐고, 우연히 떠 둔 사본으로 되살렸다).

→ 임시 파일에 쓰고 → 문법·인코딩을 확인하고 → 그때 옮긴다(move).
→ 커밋 안 된 파일을 고치기 전에는 `cp <파일> /tmp/x.bak`.
→ 오늘 자료 손실이 **두 번**이었다(작업트리 되돌리기 · 제자리 덮어쓰기).
  기전은 다르나 뿌리는 같다 — **되돌릴 수 없는 쓰기를 사본 없이 한 것**.
  훅으로는 못 막는다(파이썬 코드 안이라 명령문 글자에 안 잡힌다). 두 번 더 나면 서식검사기에 넣는다.

### 지적이 셋이면 고침도 셋이 된다 — 부류로 말한다 (26-09-11)

검토가 「정의 안 된 입력」을 셋 집어 줬더니 구현자가 그 셋만 고쳤고, **넷째가 같은 줄에 살아남았다.**
사람 잘못이 아니라 지시의 모양 탓이다. 열거는 그 자체가 범위 선언으로 읽힌다.

→ 지적할 때 **부류를 먼저 쓰고 보기를 뒤에 둔다.** "정의 안 된 이름이 셋" 대신
  "문서에 정의 없이 쓰이는 이름을 **전부** 찾아라. 지금 눈에 띄는 것은 셋이다."
→ 그리고 **「빠진 것이 있는지」를 세는 줄**을 함께 시킨다. 셈이 없으면 열거가 곧 상한이 된다.
→ 이 병은 오늘 다섯 번째다(§6 표 참조). 매번 층만 달랐다.

### 수 여러 개를 한 번에 고칠 때 까닭도 여러 개다 (26-09-11)

「54초·7,400종목·79%」를 한 번에 고치며 까닭 하나("유니버스를 걸기 전 수")를 붙였다.
종목수와 비율은 맞지만 **시간은 아니다** — 거르면 종목이 줄어드니 느려질 까닭이 못 된다.

→ 정정 각주를 쓸 때 **수마다 까닭이 같은지 따로 확인한다.** 하나로 묶이면 대개 하나가 틀린다.

### 관문 목록이 같아도 「어디서 재느냐」로 결과가 갈린다 (26-09-11)

미국 1단계에서 관문 여덟과 그 수가 백테스트와 똑같은데 통과가 **321 대 302**로 갈렸다.
다른 것은 하나였다 — RS 백분위를 **거르기 전 전 시장**으로 재느냐 **거른 뒤 생존자**로 재느냐.

값 동결 검산기는 이걸 **못 잡는다.** 수가 같은지만 보지 호출 순서는 안 본다.

→ 백테스트와 실전 도구를 맞출 때 **값 목록만 맞대지 말고 「모집단 통계를 언제 계산하나」를 따로 본다.**
→ 순위·백분위·평균처럼 **다른 종목에 기대는 수**가 있으면 거기가 갈림길이다.
→ 그리고 **비교풀 크기를 출력에 찍는다.** 안 보이면 19종목이 조용히 숨는다(실제로 숨어 있었다).

### 줄 번호로 가리키지 말고 이름으로 가리킨다 (26-09-11)

오늘 하루에 틀린 줄 인용이 **다섯** 나왔다(`us_loader.py:154`→218 · `trend_template.py:44`→43 ·
`us_loader.py:224`→225 · 검산기 「100행」→104 · 「106행」→110).
마지막 둘은 같은 세션이 바로 위쪽 docstring을 네 줄 늘려서 생겼다.
**줄 수를 바꾸는 편집은 그 아래 인용을 전부 깨는데, 완료 검사는 기억으로 뽑은 목록 위에서 돈다.**

→ `파일.py:123` 대신 **이름**을 적는다: `us_loader.avg_turnover_eok` · `trend_template.WINDOW_52W`.
  이름은 편집에 안 밀린다.
→ 줄 번호가 꼭 필요하면 **그 줄의 내용을 같이** 적는다. 그래야 밀렸을 때 보인다.
→ 파일을 고쳤으면 **그 파일을 가리키는 인용을 `grep` 으로 찾아** 확인한다. 기억으로 세지 않는다.

## 7. 나중에 할 일 (지금은 안 한다)

사용자가 「지금은 지나가고 나중에」로 정한 것만 적는다. 하나가 끝나면 줄을 지운다.

| 언제 정함 | 무엇 | 왜 미뤘나 | 다시 볼 신호 |
|---|---|---|---|
| 26-09-11 | **미국 IPO 트랙** (`find-ipo-us`) | 자료는 있다(뼈대 4,648 중 20~199봉 349종목, 평가기는 종가열과 백분위만 요구). 그런데 **27.4년 백테스트가 IPO 트랙을 한 번도 안 돌렸다** — 하네스는 vcp·cheat·power_play 셋만 쓴다. 근거 없는 후보를 실전 화면에 올리지 않는다 | 미국 실전이 자리 잡은 뒤. 순서는 ①백테스트에 IPO 트랙을 붙여 27.4년을 돌린다 ②결과가 좋으면 그때 1단계에 `--ipo-track` 을 더하고 스킬을 만든다 |

⛔ 여기 있는 것을 「빠졌다」로 읽지 말 것. **일부러 안 한 것**이고 까닭이 적혀 있다.

### 튼튼한 까닭 셋 옆의 약한 넷째는 검산을 안 받는다 (26-09-11)

미국 IPO 트랙을 안 만든 까닭으로 넷을 적었다. 셋은 맞았고 넷째가 틀렸다 —
「iRS는 모집단 통계라 2단계에서 **만들 수 없다**」고 썼는데, 뼈대에 전 종목 계열이 있으니
**만들 수 있다**. 「못 한다」가 아니라 「안 한다」였다.

**목록은 제일 센 항목의 강도로 읽힌다.** 셋이 튼튼하니 넷째도 그렇게 보였고, 아무도 안 물었다.

→ 까닭을 여럿 적을 때 **제일 약한 것을 따로 표시**하고 그것만 다시 검산한다.
→ 「할 수 없다」와 「안 한다」를 가려 쓴다. 앞은 사실 주장이라 틀릴 수 있고, 뒤는 결정이라 안 틀린다.
→ 오늘 이 구분 하나로 권고가 뒤집혔다(「없앤다」 → 「아직 안 쟀다」).

### 훑어서 고칠 때는 파일 단위로 한 번에 (26-09-11)

취소한 스킬의 잔재를 지우면서 **다섯 번 훑었고 매번 새것이 나왔다.**
까닭은 파일이 많아서가 아니라, 훑을 때마다 **거르는 말을 바꿨기** 때문이다.
거르개가 움직이니 매번 다른 부분집합이 나왔고, 나는 그때 보인 것만 고쳤다.

→ 먼저 **한 파일 안의 모든 자리를 세고**, 그 수만큼 고쳤는지 확인한 뒤 다음 파일로 간다.
→ 「고치고 다시 훑기」를 되풀이하지 않는다. 훑기는 **끝났는지 확인하는 용도**지 찾는 용도가 아니다.
→ 거르개를 바꿔야 하면 **바꾼 채로 처음부터** 다시 훑는다.

### 수도 참조다 — 이름으로 훑으면 안 나온다 (26-09-11)

취소한 스킬을 문서 본문에서는 다 지웠는데, **합격 기준에 「일곱 개」·「후보 파일 다섯」으로 살아남았다.**
`find-ipo-us` 라는 이름이 그 줄에 없으니 이름으로 훑는 도구로는 **구조상 못 찾는다.**
앞서 다섯 번 훑고도 못 찾은 까닭이 이것이다.

→ 무언가를 빼거나 더하면 **그 수를 세는 자리**를 따로 찾는다. 「N개」·「넷」·「다섯」 같은 말이다.
→ 합격 기준에 수를 적을 때는 **무엇을 세는지 이름을 나열**한다. 「여섯 개(A·B·C…)」처럼.
  그래야 항목이 바뀔 때 훑기에 걸린다.
