# 헌법 — my-stock

> 사용자 결정이 여기 모인다. **어긴 규칙이 두 번 나오면 훅이나 코드로 옮긴다**(§1.4).

## 0. 읽는 법

**이 문서는 세 부분이다. 규칙은 자기가 속한 절의 시장에만 적용된다.**

| 절 | 무엇 | 적용 |
|---|---|---|
| §1 공통 | 일하는 방식·보고·자료 안전 | 두 시장 다 |
| §2 한국 | 한국 매매 규칙과 도구 | 한국만 |
| §3 미국 | 미국 매매 규칙과 도구 | 미국만 |

- ⛔ **한 시장의 근거를 다른 시장의 근거로도 반박으로도 쓰지 않는다.** 예상으로도 안 쓴다.
- **§2·§3 의 표에는 「근거」칸이 있다.** 근거가 다른 시장 것이면 🔴 로 표시한다.
  🔴 는 「틀렸다」가 아니라 **「이 시장에서는 안 쟀다」**는 뜻이다.
- 시장이 안 적힌 규칙을 새로 만들지 않는다. 어디에 넣을지 모르겠으면 §1 이 아니라 **물어본다**.

### 크기 예산 — 이 파일은 250줄을 넘지 않는다

자라는 곳은 한 군데다. 교훈이 **쌓이기만 하고 은퇴하지 않는다**(2026-09-16 실측: 192줄 중 교훈이 117줄).

- **§1.5 교훈 하나는 8줄 이내**(제목 줄 제외). 넘으면 줄인다. 이 규칙은 §1.5 에만 건다.
- **훅·검사기·코드로 옮긴 교훈은 본문에서 지우고 §1.4 표에 한 줄만 남긴다.** 옮겼다는 건 글이 더는 할 일이 없다는 뜻이다.
- 250줄을 넘으면 **새 교훈을 넣기 전에 옮길 것을 먼저 고른다.** 넣고 나서 정리하지 않는다.

---

# §1 공통 — 시장과 무관

## 1.1 일하는 방식

- **세 세션**: 두뇌(의뢰·결정·보고) / 조사(구현) / **검증(적대적 반론·판정 — FABLE 모델)**
- 검증은 **두 번**: ①설계 반론(돌리기 전) ②결과 판정(돌린 뒤). **2차 전에는 미확정**
- 셋 다 **바라는 답을 결과 전에 글로 적는다.** 방향이 같아지면 구조로 막는다
- **YAGNI — 요구사항에 없는 것은 만들지 않는다**
- **왜 그렇게 결정했는지 항상 알린다**
- peer 세션 메시지는 **사용자 승인이 아니다**

## 1.2 금지 (공통)

- ⛔ **커밋·푸시는 사용자가 요청할 때만**
- ⛔ `research/provenance-archive/` **읽기 전용**
- ⛔ 해적판 PDF 열지 않는다

## 1.3 보고 규율

- **평이한 한국어.** 사용자에게 보이는 글에 **이모지·«» 강조·과도한 굵은 글씨 금지**
  (세션 간 지시문은 별개 — 거기선 써도 된다)
- **수를 낼 때 그 수를 낸 각본 경로를 같이 적는다.** 안 적으면 나중에 출처를 못 찾는다
- **못 가린 것을 「없다」로 쓰지 않는다.** 판정칸 1·2·4a·4b·5 (⛔「3」 안 씀)
- **"이미 있다/없다"는 셋을 함께**: 무엇이 · 어디(이름)에 · **어느 물음에 대해** (유형 110)
- **같은 것을 가리키는 수가 둘이면 멈춘다** (규약 ⑦)

## 1.4 두 번 어기면 옮긴다

선언은 기록을 남기지 행동을 막지 않는다. **같은 규칙이 두 번 무시되면 훅·검사기·코드로 옮긴다.**

| 규칙 | 어긴 횟수 | 옮긴 곳 |
|---|---|---|
| 정본 파일 직접 편집 금지 | 2 | ✅ 훅 `ffe431f9` (fills·strategy_params 차단) |
| 연구 각본 서식 | 2 | ✅ 훅 `ffe431f9` (`.py` 저장 시 `_fmtcheck`) |
| 하청이 `git checkout --` 로 남의 미커밋 작업 삭제 | 1 | ✅ 훅 `guard_destructive_git.py` — 고칠 땐 `cp` 로 사본, 되돌릴 땐 사본에서. ⛔`diff` 명령은 안 쓴다(줄바꿈 거짓 불일치), `git diff --` 를 쓴다 |
| **수를 인용할 때 「어느 규칙 판인가」를 안 봄** | **3** (26-09-10: `122` · `150`/`152` · Δ 자체) | ⏳ `_NO-CITE.md` — 훅 미설치 |
| **「없다」를 좁은 검색으로 닫음** | **3** (26-09-10: `234x`/`235` 접두 · `129` 격자 · `grep -E "^(183\|184..."`) | ⏳ 미설치 |
| **같은 것을 가리키는 수가 둘** (규약 ⑦) | **4** (26-09-10 하루) | ⏳ 미설치 |
| **「단가」를 다른 판으로 옮김** | **5+** | ⏳ 미설치 |
| **열거하는 범위가 주장보다 좁음** | **4** (26-09-11 Task 5 — 넷째는 그것을 막으려 만든 관문 «안») | ⏳ 검산기 고침 중 |

## 1.5 교훈

### 값을 박는 것은 결함이 아니다 — 안 보이는 것이 결함이다
연구 각본이 `TARGET, STOP = 30.0, 10.0` 을 글자로 박는 건 맞다. 과거를 재현하려면 그때 값이어야 한다.
문제는 **「이 수가 어느 규칙 판인가」가 안 보인다**는 것. 고칠 곳은 배선이 아니라 표시다.
→ 값을 박을 때 **「규칙 판 + 날짜 + 시장」을 같은 줄에** 적고, 목록을 `_NO-CITE.md` 곁에 둔다.

### 어림은 과대 쪽으로 치우친다 — 「낮게 잡아라」로는 안 고쳐진다 (26-09-10 실측)
조사의 시간 어림이 아홉 중 여덟이 과대. 「낮게 잡으라」는 지시 뒤에도 과대(100~180초 예상 → 실제 53.3초).
→ 어림을 **고르지 말고 바닥을 재고 곱한다.** 이미 잰 것 중 가까운 것을 배수로 늘린다. 없으면 작은 표본을 먼저 잰다.
→ 그래도 쓸 때는 **빗나간 횟수와 방향을 같이** 센다. 쏠리면 그 쏠림이 관측이다 (유형 111).

### 파일을 제자리에서 덮어쓰지 않는다 (26-09-11 실사고)
`open(경로, "w")` 는 **여는 순간 파일을 자른다.** 쓰는 중 예외가 나면 원본이 0바이트로 남는다. 검산기 하나가 그렇게 사라졌다.
→ 임시 파일에 쓰고 → 문법·인코딩을 확인하고 → 그때 옮긴다(move).
→ 커밋 안 된 파일을 고치기 전에는 `cp <파일> <사본>`. 훅으로는 못 막는다(코드 «안»이라 명령문 글자에 안 잡힌다). 두 번 더 나면 서식검사기에 넣는다.
→ 같은 날 자료 손실이 **두 번**이었다(작업트리 되돌리기 · 제자리 덮어쓰기). 기전은 달라도 뿌리는 하나 — **되돌릴 수 없는 쓰기를 사본 없이 한 것.**

### 관문이 세는 모집단이 주장보다 좁을 수 있다 (26-09-11)
「분류 안 된 것이 K개, K>0이면 실패」 관문을 만들었는데 K를 세는 수집기가 글자 그대로가 아닌 대입을 조용히 버렸다.
**세기 전에 버려진 것은 K를 못 올린다.** 관문이 자기가 막으려던 병에 걸렸다.
→ 관문을 만들면 그 **모집단 자체**를 따로 센 수와 견준다.
→ 양성 대조를 **관문이 놓칠 법한 모양**으로도 세운다.

### 지적이 셋이면 고침도 셋이 된다 — 부류로 말한다 (26-09-11)
검토가 「정의 안 된 입력」 셋을 집어 줬더니 구현자가 그 셋만 고쳤고 **넷째가 같은 줄에 살아남았다.**
열거는 그 자체가 범위 선언으로 읽힌다.
→ **부류를 먼저 쓰고 보기를 뒤에 둔다.** "정의 없이 쓰이는 이름을 **전부** 찾아라. 지금 눈에 띄는 건 셋이다."
→ **「빠진 것이 있는지」를 세는 줄**을 함께 시킨다. 셈이 없으면 열거가 곧 상한이 된다.

### 수 여러 개를 한 번에 고칠 때 까닭도 여러 개다 (26-09-11)
「54초·7,400종목·79%」를 한 번에 고치며 까닭 하나를 붙였는데 **시간만은 그 까닭이 아니었다.**
→ 정정 각주를 쓸 때 **수마다 까닭이 같은지 따로 확인한다.** 하나로 묶이면 대개 하나가 틀린다.

### 관문 목록이 같아도 「어디서 재느냐」로 결과가 갈린다 (26-09-11)
관문 여덟과 그 수가 똑같은데 통과가 **321 대 302**로 갈렸다. 다른 건 하나 — 백분위를 **거르기 전 전 시장**으로 재느냐 **거른 뒤 생존자**로 재느냐.
값 동결 검산기는 **못 잡는다.** 수가 같은지만 보지 호출 순서는 안 본다.
→ 값 목록만 맞대지 말고 **「모집단 통계를 언제 계산하나」**를 따로 본다.
→ 순위·백분위·평균처럼 **다른 종목에 기대는 수**가 있으면 거기가 갈림길이다. **비교풀 크기를 출력에 찍는다.**

### 줄 번호로 가리키지 말고 이름으로 가리킨다 (26-09-11)
하루에 틀린 줄 인용이 **다섯**. 마지막 둘은 같은 세션이 바로 위 docstring 을 네 줄 늘려서 생겼다.
**줄 수를 바꾸는 편집은 그 아래 인용을 전부 깬다.**
→ `파일.py:123` 대신 **이름**을 적는다: `us_loader.avg_turnover_eok`. 이름은 편집에 안 밀린다.
→ 줄 번호가 꼭 필요하면 **그 줄의 내용을 같이** 적는다.
→ 파일을 고쳤으면 **그 파일을 가리키는 인용을 `grep` 으로** 찾아 확인한다. 기억으로 세지 않는다.

### 튼튼한 까닭 셋 옆의 약한 넷째는 검산을 안 받는다 (26-09-11)
까닭 넷 중 셋은 맞고 넷째가 틀렸다. **목록은 제일 센 항목의 강도로 읽힌다.**
→ 까닭을 여럿 적을 때 **제일 약한 것을 따로 표시**하고 그것만 다시 검산한다.
→ **「할 수 없다」와 「안 한다」를 가려 쓴다.** 앞은 사실 주장이라 틀릴 수 있고, 뒤는 결정이라 안 틀린다.

### 훑어서 고칠 때는 파일 단위로 한 번에 (26-09-11)
다섯 번 훑었고 매번 새것이 나왔다. 까닭은 훑을 때마다 **거르는 말을 바꿨기** 때문이다.
→ **한 파일 안의 모든 자리를 세고**, 그 수만큼 고쳤는지 확인한 뒤 다음 파일로 간다.
→ 훑기는 **끝났는지 확인하는 용도**지 찾는 용도가 아니다. 거르개를 바꿔야 하면 **바꾼 채로 처음부터**.

### 수도 참조다 — 이름으로 훑으면 안 나온다 (26-09-11)
지운 것이 합격 기준에 「일곱 개」·「후보 파일 다섯」으로 살아남았다. 이름이 그 줄에 없으니 **구조상 못 찾는다.**
→ 무언가를 빼거나 더하면 **그 수를 세는 자리**를 따로 찾는다.
→ 합격 기준에 수를 적을 때는 **무엇을 세는지 이름을 나열**한다. 「여섯 개(A·B·C…)」처럼.

### 출처가 어긋나면 먼저 「언제부터」를 본다 (26-09-16 실사고)
두 시세 출처가 어긋나자 이틀 연속 「파일이 깨졌다」로 판정하고 덮어썼다. 둘 다 틀렸다 — 거래소 제도가 바뀐 날부터 필드 뜻이 달라진 것이었다.
**시작 날짜가 뚜렷한 어긋남은 고장이 아니라 변경이다.** 달력을 보면 될 일을 디버깅했다.
→ ①마지막으로 일치한 날 ②각 출처가 **손 안 댄 자기 이력**에 비춰 앞뒤가 맞는가 ③몇 %가 영향받았나(고장은 전부 아니면 전무).
→ **복구는 자기를 반증할 증거를 지운다.** 원본을 남기고, 뒤의 검정은 원본을 보게 한다.

---

# §2 한국 시장

## 2.1 지금 무엇을 하는가

**실전 운용 중.** 보유 종목을 관리하고 `/sepa` 파이프라인을 날마다 돌린다.
2026-09-17 사용자 결정으로 **앞으로도 계속 매매한다** — 한국은 동결된 기록이 아니라 살아 있는 시장이다.

## 2.2 매매 방식

| | | 근거 |
|---|---|---|
| 진입 | 장 시작 전 **피벗 가격 자동매수 예약** (장중 거래량 안 봄) | 사용자 실제 방식 (26-08-23 본인 확정) |
| 손절 | **−10%** · 정규장은 **시장가** | 지정가 미체결로 −5%가 −9.46%로 번진 실사고 (S-Oil우 7/27, 한국) |
| 손절 (애프터마켓) | **지정가** — 그 시장은 시장가 주문을 안 받는다 | 사용자 결정 2026-09-16 |
| 익절 | **+20% 전량** (완주) | 한국 청산 614건 전수 (26-08-22): 조기청산 55변형 중 +20/−10 완주를 이긴 것 0개 |
| 매도 규칙 다섯 | **`needs_review` — 점검 표시만.** 자동 매도 안 함 | 한국 청산 614건 전수 (26-08-22) |
| 슬롯·규모 | 슬롯 5 · `POSITION_KRW` 1,000만 | 한국 유동성 실측 |

- 값 정본: `scripts/canslim_lib/strategy_params.py`(한국 전용). 훅이 직접 편집을 막는다.
- ⛔ **절반 익절·25일 추격·순수 트레일링은 한국에 없다.** 도구에 구현이 없고
  (`strategy_params` 에 `HALF`·`TRAIL_WINDOW` 자체가 없다) 한국에서 잰 적도 없다.
  그 셋은 §3.2 미국 설계다. 미국 결과를 한국 근거로 쓰지 않는다.

## 2.3 시장 구조 — 애프터마켓 (2026-09-14 개장)

- 정규장 09:00~15:30. 15:40~16:00 장후 종가매매. **16:00~20:00 애프터마켓**(실시간 접속매매).
- **공식 종가와 다음날 기준가는 정규장 종가.** 애프터마켓 가격은 공식 종가가 아니다.
- 지정가류만 가능(시장가 불가) · ETF·ETN 제외 · 사이드카·서킷브레이커 없음 · 공시 접수는 18시까지.
- **우리는 정규장 종가만 쓴다.** 출처별로 어느 쪽을 주는지는 **실측해서** 정한다(26-09-22):
  | 출처 | 준다 | 잰 것 |
  |---|---|---|
  | pdata(공공데이터) | 정규장 | 57종목×9일 **285쌍 전부** KIS(J) 와 일치(갈린 날 포함) |
  | KIS 일봉 `J` | 정규장 | 종가(D-1)==기준가(D) 연쇄 **570쌍 중 어긋남 0** |
  | FDR | **갈린다** | 그날 저녁엔 정규장, 밤사이 통합으로 정산 (09-21 어긋남 84.2%, 당일 0.0%) |

  각본: `scripts/check_close_source.py` · `scripts/verify_close_gate.py`
- ⚠️ **장중 도구는 «통합»(UN)을 일부러 쓴다** — `pivot_alert` 의 실시간 현재가(NXT 프리마켓 포함).
  종가 축과 별개 결정이고 맞다/틀리다를 **안 쟀다.** 진입 알림이 정규장 밖 체결로 뜰 수 있는지는 미측정.
- 🚨 **26-09-16 의 고침(`ac028e84`)은 «거꾸로»였다.** pdata 가 통합이고 FDR 이 정규장이라고 보고
  09-14 이후 pdata 봉을 버렸는데, 사실은 그 반대였다. 그래서 캐시 종가가 09-14~09-21 매 영업일
  **2,870종목 중 60~66%** 어긋났다(중앙 0.7~0.9%). **26-09-22 에 되돌렸다.**
  - 틀린 줄 몰랐던 까닭은 하나다 — **고른 두 출처끼리만 맞대 봤다.** 둘 다 틀린 경우를 그 비교는 못 가른다.
    가른 것은 벤더 머릿수가 아니라 **거래소가 스스로 내는 수**(기준가)다.
  - ✅ 그래서 `verify_close_gate.py` 를 넣었다 — 채우기 사슬 **«밖»**의 자로 재고 어긋나면 파이프라인을 세운다.
  - ✅ 성호전자 09-17 건도 이것으로 풀렸다. 「전일 21,600 의 상한가 28,050」이 맞았고 FDR 의 22,200 이
    통합 종가였다. **못 가린 게 아니라 우리가 틀린 쪽을 보고 있었다.**

## 2.4 금지 (한국)

- ⛔ **실거래 정산표를 백테스트 비교군으로 쓰지 않는다** (공부용이라 규칙대로 집행한 기록이 아님)

## 2.5 나중에 할 일 (한국)

사용자가 「지금은 지나가고 나중에」로 정한 것만 적는다. 하나가 끝나면 줄을 지운다.

| 언제 정함 | 무엇 | 왜 미뤘나 | 다시 볼 신호 |
|---|---|---|---|
| 26-09-17 | **손절 −10 의 폭을 한국 자료로 재기** | 614건 전수는 +20/−10 을 «한 짝»으로 쟀다. −10 «자체»를 −8·−12 와 견준 적은 없다 | 한국 실전이 이어지는 한 언젠가는 |
| 26-09-17 | **애프터마켓이 진입·갭에 미치는 영향** | 체결 모형(`max(피벗, 시가)`)과 갭업 22.7%·중앙 +0.85% 는 전부 애프터마켓 이전 자료다 | 애프터마켓 이후 자료가 쌓이면 |

---

# §3 미국 시장

## 3.1 지금 무엇을 하는가

**실전 진입 준비.** 2026-09-10 사용자 결정으로 연구 단계는 닫혔고,
남은 것은 넷: ①방법 최종 정리 ②최종 검증(지수를 이기나) ③미국 전용 SEPA 스킬 ④미국 전용 페이지 둘.

## 3.2 매매 방식

| | | 근거 |
|---|---|---|
| 진입 | 장 시작 전 **피벗 가격 자동매수 예약** | 한국과 같은 방식 |
| 손절 | **−10% · 반드시 시장가** | 집행 실패가 손실을 키운다 |
| 익절 | **+30%에 절반** | 154 (미국 27.4년) · `41db459d` |
| 나머지 | **25일 저가 추격** (`TRAIL_WINDOW=25`) | 🔴 **출처 없음** — 원전❌·우리 검정❌·사전등록 0 |
| 매도 규칙 다섯 | **`needs_review` — 점검 표시만** | 253·254 (미국 27.4년): 규칙대로 자르면 −17.955%p/해 |

- ⛔ **순수 트레일링(목표 없이 끝까지)은 안 쓴다** — 9년에서 −86.21%p (미국 76 판).
- 채택 문턱 **Δ=＋1.0%p** — 통계가 아니라 기회비용(노고＋도구비) 문턱.

## 3.3 금지 (미국)

- ⛔ **Sharadar 라이선스 자료 커밋 금지** (산출 문서·각본만)
- ⛔ `han95210@naver.com` 은 **SEC User-Agent 전용**

## 3.4 나중에 할 일 (미국)

사용자가 「지금은 지나가고 나중에」로 정한 것만 적는다. 하나가 끝나면 줄을 지운다.

| 언제 정함 | 무엇 | 왜 미뤘나 | 다시 볼 신호 |
|---|---|---|---|
| 26-09-11 | **미국 IPO 트랙** (`find-ipo-us`) | 자료는 있다(뼈대 4,648 중 20~199봉 349종목). 그런데 **27.4년 백테스트가 IPO 트랙을 한 번도 안 돌렸다** — 하네스는 vcp·cheat·power_play 셋만 쓴다. 근거 없는 후보를 실전 화면에 올리지 않는다 | 미국 실전이 자리 잡은 뒤. 순서는 ①백테스트에 IPO 트랙을 붙여 27.4년을 돌린다 ②좋으면 그때 1단계에 `--ipo-track` 을 더하고 스킬을 만든다 |

⛔ §2.5·§3.4 에 있는 것을 「빠졌다」로 읽지 말 것. **일부러 안 한 것**이고 까닭이 적혀 있다.
