"""SEPA 매매 전략 최적값 — 단일 진실 공급원(single source of truth).

시행착오·백테스트로 확정한 정본값. 봇(autobuy)·백테스트·검증 스크립트는 여기서 import해서 쓰고,
값을 바꿀 땐 여기서만 바꾼다. (검출기 VCP/3C/PP 최적값은 여기가 아니라 각 모듈의 DEFAULT_PARAMS가 정본.)
"""
from __future__ import annotations

# ── 손익비 (익절/손절 %) ──────────────────────────────────
# ★ 2026-09-02 사용자 결정 — 익절 **+20 → +30**. 손절 −10 은 유지.
#   근거: 154(미국 27.4년) — 같은 판·같은 씨앗 60개에서 «세금 스위치»만 갈아 끼워 잼.
#         **«세금 전»에도 +30 이 +20 을 48/60 으로 이긴다 (귀무 p = 1.6e-06).**
#         ⇒ 「세금 시점」 때문이 아니라 **«규칙 자체»가 낫다** → 원전과 충돌 없음
#            (원전으로 «전해지는» 말: 「거래회전율이나 세금 «때문에» 매도를 결정하는
#             일은 없다」 — ⚠️ canon/minervini-principles.md 에서 «못» 찾음 · 출처 «미확인»)
#   ⛔ 못 쓰는 말: 「+30 이면 지수를 이긴다」(152 가 «못 가린다»로 닫음) ·
#      「세금을 아끼려 +30」(원전이 막음) · 「+30 이 «최선»이다」(132 는 «vs +20» 만 쟀음)
#   🚨 **알고 치르는 값** — «돈»으로는 세금이 격차를 좁힌다(중앙 4,730 → 2,835만).
#   🚨🚨 **아직 «대조 안 한» 것** — 위 근거는 전부 **「+30 에 «절반» 팔고 나머지는 추격」**
#        (하네스 `HALF=0.5`)이다. **「+30 «전량» 매도」는 «잰 적이 없다».**
#        사용자님 실제 집행이 «전량»이라면 이 근거는 그대로 옮겨지지 «않는다».
#   (옛 20.0/10.0 은 6년 검증(2020-2026, 한국)에서 온 값. 그 전 15.0/7.5 는 R-분석)
TARGET_PCT: float = 30.0
STOP_PCT: float = 10.0

# ── 진입 거래량 문턱 (동시간대-대비 페이스, autobuy.vol_curve 정규화) ──
# 1.5 = 사용자 확정(어중간 거래량 승자도 매수). 검증상 ≥3~5가 선별력 최고지만 승자 놓침 방지 우선.
# 옛 signals.evaluate_entry 기본값 3.0을 대체.
VOL_PACE_MIN: float = 1.5

# ── 단기-창 거래량 스파이크 (누적 pace가 굼떠서 놓치는 "이른 돌파+늦은 거래량" 대응) ──
# spike_pace = 최근 W분 거래량 / (avg50 × [C(t)−C(t−W)]). 누적과 OR로 결합(어느 하나 충족이면 거래량OK).
# 시작값(백테스트로 보정 예정). 코스맥스엔비티 7/9서 매수존 순간relvol ~4 관측 → 3 시작.
SPIKE_MIN: float = 3.0
SPIKE_WINDOW_MIN: int = 5

# ── 추격 상한 (피벗 대비 %; 초과 시 매수 금지) ──────────────
# 미너비니 규칙(피벗 코앞에서만 매수, 돌파 후 익스텐디드는 안 쫓음). 변동 없음.
CHASE_MAX_PCT: float = 3.0

# ── 동시 보유 슬롯 수 ─────────────────────────────────────
# 포트폴리오 검증: 4~5 최적(5가 수익 최고·MaxDD 최저). 과분산(10~15) 열위. 옛 10 대체.
SLOTS: int = 5

# ── 기준 포지션 금액 (원; 실제 슬롯 단위) ──────────────────
# 실제 슬롯 단위(사용자 매매 기준, 26-08-17 3,000만→1,000만 정정 — 매수추천 N/M·주문표가 참조).
# 매수 추천 리스트가 "이 금액으로 살 때 청산 부담률 N%·분할매도 시작 금액 M"을 계산할 때 사용.
# 투자금을 늘리면 여기만 올리면 청산 부담 등급 전체가 따라 움직인다.
# 에스에스알(포지션=일거래량 28%)·로스웰(29%) 청산 불능 사고 후 도입(26-08-08).
POSITION_KRW: int = 10_000_000

# ── 국면 규칙 (등가중 breadth 지수 이동평균) ───────────────
# 등가중 breadth 지수 > REGIME_MA일 이평(상승중)이면 매매 ON. 20MA가 최선(50·200MA 열위).
# 사용처: autobuy.signals.is_uptrend(index, ma=REGIME_MA).
REGIME_MA: int = 20
